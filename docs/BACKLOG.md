# BACKLOG — the single list (D17)

**This is the only backlog.** Before this file existed the same items were
scattered across `PHASE_LOG.md` "classified but not implemented" tables,
`07-IMPLEMENTATION-STATUS.md` limitation bullets, `FINAL-FULL-PROJECT-AUDIT-PLAN.md`
severity rules and four "R-5: object storage · CSV export · …" lines that had
been copy-pasted forward through five phases. Nothing here is new work
invented for the file; every row cites where it came from.

Created **2026-09-01** at commit `95c34bf`, under D17 §10.
Last updated **2026-09-08** after **P11** — see
[PHASE_LOG.md](PHASE_LOG.md). Resolved rows keep their ID, their history and
their reason; nothing is deleted.



## P13 production readiness (2026-09-10)

From `docs/P13-PRODUCTION-READINESS-REPORT.md`. Verdict **NO-GO for
production** — every repository-side requirement is met; the blockers are
external.

| ID | Item | Owner | Status |
|---|---|---|---|
| **P13-1** | Wildcard DNS `*.sikhon.systems` | owner / DNS provider | **EXTERNAL — OPEN** |
| **P13-2** | Wildcard TLS certificate | owner / CA | **EXTERNAL — OPEN** |
| **P13-3** | `WILDCARD_DNS_READY=1` — set only AFTER 1 and 2; the flag fails closed by design | owner | **BLOCKED on 1,2** |
| **P13-4** | SMS aggregator contract + `SMS_*` credentials. The SSL Wireless adapter is written and tested. **`SMS_TEST_RECIPIENTS` must be set before the first dispatch** or real guardians are texted. | owner / commercial | **EXTERNAL — OPEN** |
| **P13-5** | VAPID keys (`scripts/generate-vapid-keys.mjs`) + real push delivery | owner | **EXTERNAL — OPEN** |
| **P13-6** | `ALERT_WEBHOOK_URL` — until set, the monitor logs but pages nobody | owner | **EXTERNAL — OPEN** |
| **P13-7** | Production backup **schedule** (RPO). Restore is rehearsed and passing: 27 tables and 16 tenants identical, RTO 5.9s. | owner / host | **EXTERNAL — OPEN** |
| **P13-8** | Production secrets: `JWT_*`, `PLATFORM_API_KEY`, `ACTIVATION_PEPPER`, `PII_MASTER_KEY_V1`, `CRON_SECRET`, `SERVICE_API_KEY` | owner | **EXTERNAL — OPEN** |
| **P13-9** | **A real pilot institution** (B-5). Without one the pilot journey cannot be run at all. | owner | **EXTERNAL — OPEN** |
| ~~**P13-10**~~ | **FIXED 2026-09-10** — five production-runtime variables were read by services and undocumented in `deploy/env.example`; worst was `SMS_WORKER_TENANT_IDS`, where empty means no school's queue is drained and nothing sends. Now documented with consequences, and guarded by `env-template.test.ts` with a negative control. | — | **CLOSED** |

## P12 final audit findings (2026-09-10)

From `docs/P12-FINAL-AUDIT-REPORT.md`. No CRITICAL, no MAJOR. Verdict
**CONDITIONAL GO** - conditional on external infrastructure, not on these.

| ID | Finding | Severity | Where | Status |
|---|---|---|---|---|
| ~~**P12-1**~~ **FIXED 2026-09-10** — one shared `formatAcademicYear()` in `packages/ui-core/src/format.ts`, applied at **15 render sites across 11 files** (the audit had found 4). Idempotent, because the database holds both `2026` and `২০২৬`. New `academic-year-numerals.test.ts` checks what the literal-scanning guard structurally cannot, with a negative control. Verified on live data: 0 Latin-digit nodes across five routes; home/academic/import read `শিক্ষাবর্ষ ২০২৬`. Was: The academic year rendered in **Latin digits** inside Bangla sentences - "শিক্ষাবর্ষ 2026" - on `#/home`, `#/academic`, `#/import`, `#/exams`. The year is a DATABASE value interpolated raw at `academic-view.ts:388,484`, `import-view.ts:305`, `principal-home-view.ts:146`, `structure-forms.ts:298`. `bnNum()` exists and is used elsewhere; `import-view.ts:305` passes the STEP through `bn()` on the same line while leaving the year alone. `bangla-numerals.test.ts` scans source literals, so it structurally cannot catch digits arriving from PostgreSQL. | MINOR | PWA | **OPEN** |
| ~~**P12-2**~~ **FIXED 2026-09-10** — `students-view.ts` now calls the shared `pageHeader()`; `h1: 1` verified in the browser. `heading-hierarchy.test.ts` guards it with a negative control built from the shape that shipped. Was: `#/students` rendered **no `h1`** - the document outline starts at `h2` ("শিক্ষার্থী খুঁজুন"). All 23 other routes have exactly one `h1`. A screen-reader user landing here gets no page heading. | MINOR | PWA | **OPEN** |
| ~~**P12-3**~~ **FIXED 2026-09-10 at the cause** — the tab bar was the SYMPTOM (it is `position: fixed`, so it can only be as wide as the document). The cause was `.btn-secondary { margin: 0 var(--s-4) }` — a COLOUR variant carrying a layout margin — meeting `.ui-button-row > * { width: 100% }` below 1024px. Four rules already existed only to undo that margin, so it was removed from the variant rather than patched a fifth time. No `overflow-x: hidden`. Verified overflow 0 at **320/375/390/430px**. Was: **16px horizontal overflow** on `#/academic` at 375px: the bottom tab bar measures 391px against a 375px viewport, reproducible across repeated probes. On `#/home` it measures exactly 375px. | MINOR | PWA | **OPEN** |
| ~~**P12-4**~~ **RESOLVED 2026-09-10 — the finding was wrong, and the real defect was worse.** 038 and 076-079 need no down file: the chain drops the base tables in 001-037 and CASCADE takes the rest; `080_platform_operators.down.sql` already documented this. Creating five files would have been inventing work, so they are documented in `db/rollback/README.md` instead. **The real defect:** the README's loop globbed `*.down.sql`, which matches only 48 of the 75 rollback files — an operator following the runbook would have silently skipped **27** and stopped with a half-dropped schema. CI was never red because `database.yml` globs `*.sql`. Runbook and tested reality now agree. Was: **5 of 80 migrations have no rollback script** - 038, 076, 077, 078, 079, four of them the most recent. A bad deploy of those cannot be rolled back by the documented path. | MINOR | db | **OPEN** |
| ~~**P12-5**~~ **FIXED 2026-09-10** — removed by explicit id, not by pattern: **21 → 16 tenants**, with a before/after slug diff proving exactly those five went and nothing else moved. Zero `audit-`/`probe`/`p12-` tenants remain; no customer data touched. Was: **5 stale tenants** in the development database from earlier audit runs (`audit-onb-*`, `p7-probe-*`). Development only, but it is how B-119's 294-tenant leak started. This audit created 3 and removed all 3. | MINOR | dev env | **OPEN** |
| **P12-E** | **EXTERNAL, not code:** wildcard DNS, wildcard TLS, subdomain routing and real push delivery are all recorded `blocked` in `production-evidence.json`; the SMS aggregator contract does not exist. Without these a school cannot be onboarded onto its own address or send a guardian a message. | BLOCKER (external) | infra | **OPEN** |

## How to use it

- **One row, one ID.** IDs are permanent. A resolved item keeps its ID and its
  row; it is never deleted (D10/D17). The historical narrative stays in
  [PHASE_LOG.md](PHASE_LOG.md); this file carries only the current state.
- **Category is about *when*, priority is about *how much*.** An item can be
  EXTERNAL DEPENDENCY and still be a pilot blocker — that is the normal case
  here, and the two columns say so independently.
- **Blocker** means: a pilot school cannot run without it. Not "important".
- When an item is fixed: set status **RESOLVED**, record the commit, and
  append the finding to `PHASE_LOG.md`. Do not remove the row.

## Status vocabulary

`OPEN` · `IN PROGRESS` · `BLOCKED` · `RESOLVED` · `RECLASSIFIED` ·
`SUPERSEDED` · `DEFERRED`

---

## 1. MUST FIX BEFORE PILOT

> **2026-09-02 — the Owner SaaS Operations Audit added four more, and they outrank these:**
> `B-46` exam creation · `B-47` fee amounts · `B-48` payment/receipt · `B-49` routine/room creation.
> A school that onboards successfully can take attendance and then do nothing else. See §3 below.

**Still open: `B-1`, `B-2`, `B-3`, `B-4`, `B-5`.** `B-6` is resolved and keeps
its row — the category says when an item *had* to be fixed, not whether it
still is, and deleting a closed row loses the reason it was ever opened.

| ID | Item | Why it matters | Phase | Priority | Blocker | Depends on | Status | Source |
|---|---|---|---|---|---|---|---|---|
| **B-1** | An SMS aggregator contract, and `OTP_SENDING_ENABLED=true` | Every SMS path in the product is built, suppression-aware and tested against a **fake** aggregator. Nothing has been sent to a real handset. | R-8 | HIGH | **Yes** for SMS login; **No** for a pilot that uses activation codes | Commercial contract (**external**) | **BLOCKED** — see also B-15 | PHASE_LOG R-8 "gates still open" |
| **B-2** | Web push observed on a real device | VAPID keys, subscription storage and the send path are implemented and unit-tested; no notification has ever arrived on a phone. Production now has a valid cert, so the last technical blocker is gone. | R-9 | HIGH | No | A pilot tenant with one real user | **OPEN** — est. 10 minutes once B-5 exists | PHASE_LOG R-8 |
| **B-3** | `ALERT_WEBHOOK_URL` set to something a human reads | `/ops/monitor` runs every 15 minutes on production and evaluates correctly. With no webhook it logs to a file nobody is watching, so an outage is discovered by a school phoning up. | R-8 | HIGH | **Yes** | One env var + a destination | **OPEN — but the pipeline is now proved.** `scripts/alert-rehearsal.mjs` (P-pilot-hardening, 7/7): the real monitor endpoint, a genuinely firing `database_unavailable`, a real POST, and a real HTTPS listener receiving it 21ms later with the alert id, severity, environment and recover text. What is left is exactly the two things a script cannot do — a URL pointing somewhere a person reads, and the observation that it arrived. Recorded as `rehearsed` in production-evidence.json; it closes no gate. | PHASE_LOG R-8; P-pilot-hardening |
| **B-4** | Cross-tenant isolation probe **on production** | RLS posture was verified on the production database directly (227 policies, 0 tenants visible without context). The *probe* — tenant A's token reaching for tenant B's data through the live API — needs two tenants and production has none. | R-8 | HIGH | **Yes** | B-5 | **BLOCKED** on B-5 | PHASE_LOG R-8 |
| **B-5** | A first pilot institution | 0 institutions exist in production. Four gates below cannot be closed without one. | R-8 | HIGH | **Yes** | Owner / commercial | **OPEN** | PHASE_LOG R-8 |
| **B-6** | Class / section **edit** UI | `structure.ts` had GET and POST only. A section created with a typo could only be corrected with SQL, and the pilot runbook calls that a blocker. **The gap was never in the database** — migration 042 has allowed UPDATE on both tables for four roles since R-3. | Closure pass | HIGH | ~~Yes~~ | — | **RESOLVED** 2026-09-01 · `PATCH /api/v1/ops/structure` + a P2 drawer on the section-detail and per class-group screens. Renaming only: level, stream, group, parent class and year are refused, because they re-base every enrolment beneath them. 12 DB tests, browser-verified including the capacity refusal. | PHASE_LOG R-8 cleanup audit; closure pass |

### The audit's six MUST-BEFORE-PILOT items, after P-pilot-hardening

The final audit's §25. Kept here beside B-1..B-5 because they overlap and a
reader should not have to hold two lists.

| | Item | State 2026-09-02 | What is left |
|---|---|---|---|
| **M1** | Deactivation revokes; refresh checks status | **CLOSED** (`0d8d32c`) | — |
| **M2** | Production 048 → 064 | **PROCEDURE WRITTEN AND REHEARSED, NOT RUN** | A maintenance window and host access. See [13-MIGRATION-CATCHUP.md](13-MIGRATION-CATCHUP.md), and read its §0 first |
| **M3** | The URL a school is given | **OWNER DECISION** | There is **no wildcard DNS record** — verified, not assumed. `?tid=` works today. Subdomains need a wildcard A record plus a DNS-01 certificate |
| **M4** | One real SMS delivered | **BLOCKED** — same as B-1 | An aggregator contract |
| **M5** | One alert reaches a human | **BLOCKED** — same as B-3 | A webhook URL. The pipeline itself is proved |
| **M6** | Teacher attendance minimal loop | **CLOSED** (`e92e9cb`, `1a2e3d1`) | B-45 is the follow-on, and is not a pilot blocker |

## 2. SHOULD FIX BEFORE PILOT

**Still open: `B-9`, `B-10`.** `B-7` and `B-8` are resolved — `B-7` in P5,
by migration 050.

| ID | Item | Why it matters | Phase | Priority | Blocker | Depends on | Status | Source |
|---|---|---|---|---|---|---|---|---|
| **B-7** | Guardian **unlink** | `guardians.ts` has no DELETE, and migration 042 denies it at the database (`guardianship_delete_scope … USING (false)`) **deliberately** — its own comment says a family relationship should not have a delete button. **Audited in the closure pass; the constraint is exact:** `guardianships` has no `ended_on`/`revoked_at` column, so the model cannot express "this link ended". A soft-end column is the right answer and is not small — `guardianships` is read at **21 sites across 11 files**, including `sms-svc/dispatch.ts` and `ops-svc/api/notices.ts`, so a revoke that misses one keeps sending a stranger the child's absence texts; and both unique constraints plus `app.my_ward_ids()` / `app.can_see_student()` (read by RLS across the schema) must change with it. | P5 | HIGH | No | — | **RESOLVED** 2026-09-01 · migration 050. `revoked_at`/`revoked_by`/`revoked_reason`, all three or none; **nothing deleted**. Eleven of the twelve read paths closed by ONE restrictive SELECT policy rather than eleven edits — including `sms-svc/dispatch.ts`, whose query was not changed at all — and the three the policy cannot reach (two SECURITY DEFINER, one other DB role) rewritten explicitly. Refuses to remove the last contactable guardian of a student with no phone. 16 DB tests, twice; browser-verified. | PHASE_LOG R-8 cleanup audit; closure pass; P5 |
| **B-8** | ~~What `doLogout` does about the read-through caches~~ **RESOLVED 2026-09-01** | Every screen caches its last answer in a `shikhon_*` localStorage key. On a shared device the next person to sign in can be painted the previous user's data from cache before the network answers. P4 fixed this for the **demo role picker**, which is the surface a stranger can reach; a real logout still leaves the caches. Deliberately not fixed in P4: the sync outbox lives alongside them and may hold a teacher's unsent attendance, and losing that is worse than a stale screen. | Closure pass | HIGH | No | — | **RESOLVED** · `apps/pwa/src/local-data.ts` classifies local state in four tiers; logout clears session + screen caches + the Cache API and **never** touches the IndexedDB outbox. The question that made P4 defer it — what happens to a teacher's unsent attendance — is answered by the sync engine instead: a session flushes only ops matching its own `tenantId`/`actorId`, so another person's work is preserved rather than posted under the wrong token. A cache-rewrite race found in the browser is closed by a synchronous final sweep. 14 tests + three browser transitions. | PHASE_LOG P4, closure pass |
| **B-9** | AI soft-limit notification | `soft_limit_notified_at` is stamped at 80%; nothing is wired to R-2's notification system, so a principal learns of the limit by being refused. | P5/P6 | MEDIUM | No | — | **OPEN** | 07 §9j |
| **B-10** | Operator SSO for the Platform Console | Console sign-in is two pasted secrets. Expected at R-7, not built. | P7 | MEDIUM | No | — | **OPEN** | 07 §9j |

## 3. NICE TO HAVE

**Still open: `B-11`, `B-12`, `B-13`, `B-14`, `B-16`, `B-36`, `B-37`, `B-40`, `B-41`, `B-42`, `B-44`, `B-45`.** `B-15`, `B-39` and `B-43` are resolved.
`B-30` and `B-31` (below, opened by the closure pass) are resolved by P5-0.

| ID | Item | Why it matters | Phase | Priority | Blocker | Depends on | Status | Source |
|---|---|---|---|---|---|---|---|---|
| **B-11** | Audit export ~~/ actor-name resolution~~ | The audit viewer exists and is reachable (`audit-view.ts`, in the nav for principal and IT admin). **Half of this row was already false when it was written.** `services/ops-svc/api/audit.ts` has resolved actor names since `9ada3e0` (2026-08-29, R-3 gap closure) — it `LEFT JOIN users u ON u.id = a.actor_id` and returns `actor_name`, and `audit-view.ts` renders `actor.nameBn` and offers an actor facet filter. This file was created 2026-09-01, three days later, and carried the claim forward unchecked. **What remains open is EXPORT only**, which is P11's subject. Corrected 2026-09-08 during the P11 readiness audit. | P5 | LOW | No | — | **RESOLVED 2026-09-08 (P11)** · `GET /api/v1/ops/export?dataset=audit`. Timestamp, resolved actor name, action, entity and the before/after states, flattened to readable `key: value` pairs. The redaction is the VIEWER's function, imported rather than restated — a second redactor beside it is how a masked phone stops being masked in one of the two places, and the file is the copy that leaves the building. It also masks uuid-shaped VALUES, which the key-name rule cannot catch: `teacherId` looks as innocuous as `reason` and its value is a primary key. Found by fetching a real export in a browser, not by a test — the seeded rows had no such field. | PHASE_LOG R-8 cleanup audit |
| **B-12** | CSV export endpoints | `toCsv()` exists in the codebase and nothing calls it. Listed under R-5 for five phases without being built. | R-5 / P6 | LOW | No | — | **RESOLVED 2026-09-08 (P11)** · Ten datasets across three services, plus an offboarding manifest. `toCsv` itself is untouched and still serves the import error list; the exporters use `csvCell`/`csvLine` beside it — same quoting, same BOM, plus row-at-a-time emission and a spreadsheet-formula guard that a five-row error list never needed. | PHASE_LOG R-5 → R-8 |
| **B-13** | Multi-card ID layout | One ID card per sheet today. Cosmetic; a school prints more paper. | R-5 | LOW | No | — | **OPEN** | PHASE_LOG R-5 |
| **B-14** | Attendance date-range filter, and search type-ahead | Both are conveniences over working screens. | R-6 / P6 | LOW | No | — | **OPEN** | PHASE_LOG R-6 |
| **B-15** | A student-facing routine card | §4 lists "today's classes" first and the product could not answer it: `GET /rms/routine` wraps `app.teacher_day(claims.sub)`, so a student got their own empty teaching day. P4 left the card out rather than fabricate a timetable. | Closure pass | MEDIUM | No | — | **RESOLVED** 2026-09-01 · migration 049 `app.student_day` + `GET /academics/myroutine`. Section-scoped, parallel-block filtered through `student_subjects`, substitutions resolved to the covering teacher, gated by `app.can_see_student` so a guardian reads their own child and nobody reads a classmate. 18 DB/API tests + 10 UI tests. | PHASE_LOG P4, closure pass |
| **B-16** | Money formatting | Carried in the R-5 deferral line since R-5 and never closed. | R-5 | LOW | No | — | **OPEN** | PHASE_LOG R-5 → R-8 |
| **B-36** | **The fixture advisory lock has no timeout** | B-35's fix turns a fixture race into a wait — correctly. But `pg_advisory_lock` blocks forever, so ONE wedged suite stops every other DB suite in the repository with no diagnosis: the holder sits `idle` on `ClientRead` and the rest report nothing at all. It cost two ~40-minute stalls in P7 before the cause was found by running workspaces one at a time. A bounded wait (`lock_timeout`) that fails with "another suite has held the fixture lock for N seconds" would turn an invisible hang into a named failure. Does not change what the lock protects. | P7 | MEDIUM | No | B-35 | **RESOLVED 2026-09-08 (pre-pilot pass)** · `SET lock_timeout = 90s` before `pg_advisory_lock`, and the failure names the situation: “fixture lock not acquired within 90s — another test process is holding it. Run the DB suites one workspace at a time to find the holder.” Landed alongside B-66's watchdog, which bounds the other direction — a suite that TAKES the lock and dies. | PHASE_LOG P7 |
| **B-37** | **The routine/timetable has no entry in `service_catalogue`** | The catalogue (migration 051) has thirteen services and RMS is not one of them, so routine generation cannot be sold as part of a plan, disabled for one school, or put in maintenance — while notices, results and attendance all can. It may well be deliberate (a timetable is arguably core, not an add-on), but nothing records the decision, and P7's service gate silently leaves `rms-svc` ungated as a result. | P7 | LOW | No | — | **OPEN — needs a product decision** | PHASE_LOG P7 |
| **B-39** | **Platform operators have no directory, so the audit trail cannot name who acted** | `audit.platform_access.admin_id` is a JWT subject, not a `users` row — 843 rows against one id in the development database, and none of the five most active ids resolve to a person. P7's audit tab therefore shows WHAT, WHY and WHEN and deliberately omits WHO, because printing the uuid would break "never expose raw UUIDs" and a truncated one would look like an identity while being a fragment. That is honest but it is not accountability: a console built so that every dangerous act carries a reason cannot currently say whose reason it was. Needs a platform-operator identity — even a small table with a name per issued credential. | P7 | MEDIUM | No | — | **RESOLVED 2026-09-08 (P10-5)** · `b509a32`, migration 080. Exactly what the row asked for and deliberately no more: `platform_operators` is a NAME per issued credential and holds no password, hash or token — the credential is still minted out of band, so the console does not become worth stealing. `readAudit` LEFT JOINs it, so the tab now shows WHO; an unnamed credential reads “নাম নেই” rather than as nobody, and the actor uuid still never leaves the server, so “never expose raw UUIDs” holds unchanged. Revocation is enforced in `authorize()` on the NEXT request, not merely greyed out in a list; an unknown credential is allowed through on purpose, because this table names credentials and does not issue them, and treating unknown as revoked would have locked out whoever held the only one the day it shipped. Self-revocation is refused at both the handler and the function. Revoked rows are KEPT: the audit rows they wrote must stay resolvable for as long as those rows do. **Checked before building a second identity table:** `users.tenant_id` is `NOT NULL` and every RLS policy is written against it, so an operator — who belongs to no school — cannot live there without weakening every school's isolation. **A hole opened and closed in the same commit:** the new table inherited migration 010's blanket `GRANT … ON ALL TABLES … TO shikhon_app` and had no RLS, so the role serving every tenant request could read the list of people who can suspend any school in the country. Caught by this workstream's own SQL test, fixed with REVOKE + FORCE RLS, and now also probed by `migration-status` (`2a52a49`), which treats FORCE RLS rather than the table as 080's sentinel. 10 SQL + 7 API + 8 view tests. | PHASE_LOG P7 §33, P10 |
| **B-40** | **Bulk operations across institutions** | P7 §35 asked for them and they are **not built** — deliberately, rather than by omission. Every control in the console acts on one school, states its consequence, and is confirmed individually. The bulk version of that is a screen whose most natural use — suspend these eleven — is the most destructive action the product can take, and the phase that built the console is the wrong phase to add an untested many-school mutation to at the end of. Nothing becomes SQL-only as a result: every school can be operated from its own screen, so this is convenience, not capability. If it is built, the shape that survives review is an explicit selection (never "all matching this filter"), a preview naming every affected school, one written reason for the batch, and one audit row **per school** rather than one for the batch. | P7 | LOW | No | — | **DEFERRED — POST-PILOT, with a scale trigger.** P8 §20 asked whether the current institution count justifies it. It does not: the console lists **62 schools in a development database and 0 in production**, and at that size the per-school screens are not the bottleneck. The trigger is roughly **50 real institutions**, or the first time an operator needs the same commercial change applied to more than about ten schools in one sitting — a term-boundary invoice run being the likely first. Until then the risk is one-sided: the natural first bulk action is suspension, and a mis-selected bulk suspend is the most destructive thing this product can do. | PHASE_LOG P7 §35, P8 §20 |
| **B-41** | **Two table systems and two card systems, both live** | `.data-table`/`.table-scroll` is hand-built in **11** view modules; `.ui-table` is emitted by `dataTable()` and used by **23**. (Recorded as 10 and 19; re-counted 2026-09-08 — both grew, so the divergence is marginally worse rather than stale.) They are not visually equivalent — 14px with `td { text-align: right }` against 13px with `text-align: end` only on `[data-numeric]` — and `my-attendance-view` and `students-view` each render BOTH, so one screen shows two differently-sized tables side by side. Same shape for `.card` (62 applied sites) beside `.ui-card` (23 modules calling `card()`), whose rule bodies differ only in padding and in spelling the same border two ways. Neither is removable: both halves are live, and deleting either strips 62 elements or breaks 10 screens. The fix is converting the hand-built call sites to the primitive, which is a screen-by-screen change with real regression surface and no user-visible benefit per screen — the benefit is only at the end. | P8 | MEDIUM | No | — | **OPEN — recorded, deliberately not done in a cleanup phase** | PHASE_LOG P8 §2 |
| **B-42** | **`scripts/check-secrets.mjs` looks for a role name production does not have** | The detector fires on a Neon-era connection-string shape. Production is a VPS with a Docker PostgreSQL and different role names, so the check passes on a repository that could contain a real production credential. It is a live security detector that must be WIDENED, not removed — which is why it is a backlog row and not a P8 edit: getting the pattern right needs the production connection-string shapes, and guessing at them would produce either false alarms nobody reads or the same silence with more confidence. | P8 | MEDIUM | No | Production connection-string shapes | **OPEN** | PHASE_LOG P8 §1 (stale-references audit) |
| **B-43** | **Four DB-backed suites can interleave with the locked ones** | `services/academics-svc/test/api.test.ts`, `rms-svc/test/solve-cross-shift`, `solve-doubles` and `solve-parallel` import the fixture-lock helper without taking the lock, or do not import it at all, while writing the same shared database as the 27 suites that do. B-35 fenced the suites that participate; these are the ones outside the fence. They have not been observed to fail — which is exactly the property B-35 warns about, since the failure mode is a scatter of assertion errors with no common cause rather than a clean break. | P8 | MEDIUM | No | B-35 | **RESOLVED 2026-09-02 (P-pilot-hardening)** — and it was FIVE files, not four: `services/academics-svc/test/api.test.ts` plus the four rms suites. All five imported `lockFixtures`, never called it, and called `unlockFixtures()` in `after` — which returns early when no lock was taken, so the omission was silent. It stopped being theoretical during this phase: one rms file reported 57 tests where 62 were expected, with no individual test named, then passed three times in a row on its own. All five now take the lock, and `packages/server-core/test/fixture-fencing.test.ts` asserts the rule at SOURCE level, because re-running a suite cannot prove it is fenced. | PHASE_LOG P8 §13; P-pilot-hardening |


### Opened by the Final Owner-Level SaaS Operations Audit (2026-09-02)

The first four are the audit's headline: a school that onboards successfully can import
students and take attendance, and then cannot examine, bill, timetable or take money. None of
them appeared in this file before today, which is itself the finding — this document's header
calls it "the only backlog". Full evidence:
[FINAL-OWNER-SAAS-OPERATIONS-AUDIT.md](FINAL-OWNER-SAAS-OPERATIONS-AUDIT.md).

| ID | Item | Why it matters | Phase | Priority | Blocker | Status | Source |
|---|---|---|---|---|---|---|---|
| **B-46** | ~~**No code anywhere creates an exam**~~ **RESOLVED 2026-09-02 (`7f9c7be`)** | `grep -rn "INSERT INTO exams"` across `services/` and `packages/` excluding tests returns **0**, and **0** `app.*` functions insert into it. The whole exam → grade → GPA → rank → publish chain is built, tested, gated and **unreachable**. Blocks class tests, merit lists, tabulation and results. | — | **HIGH** | **Yes** | **OPEN** | Owner SaaS audit §18 |
| **B-47** | ~~**`fee_structures` has no writer**~~ **RESOLVED 2026-09-02 (`577162e` + `ccf7226`)** | 0 writers in code, 0 in SQL functions, and **0 rows across 112 tenants**. The monthly invoice run joins this table, so it produced zero invoices forever. Closed by P0/A2: `finance-svc/api/feestructures.ts` (GET/POST/PATCH/DELETE), migration 067's write scopes and `uq_fee_structure_scope`, and `apps/pwa/src/fee-structures-view.ts`. A test asserts the downstream — a price set through the screen produces a real invoice. | — | **HIGH** | **Yes** | **RESOLVED** | Owner SaaS audit §18 |
| **B-48** | ~~**No payment can be recorded and no receipt issued**~~ **RESOLVED 2026-09-03** (`ddbab64` + `cb34338`) | The only `INSERT INTO payment_receipts` and the only caller of `app.apply_payment_to_invoice` were inside the MFS webhook; `POST /finance/pay` returns 503. Closed by `POST /api/v1/finance/payments` — applies the payment, issues the receipt and posts the balanced ledger pair in one transaction, with the collection control on the invoice screen. Partial payments supported (a receipt per payment), overpayment refused by name. `/finance/pay` stays kill-switched: the online flow needs merchant credentials this deployment does not have. The second half — on a receipt-number collision the webhook applied the payment and **silently issued no receipt** — is also fixed: `app.next_receipt_no()` allocates under an advisory lock and the `ON CONFLICT … DO NOTHING` is gone. | — | **HIGH** | **Yes** | **RESOLVED** | Owner SaaS audit §18 |
| **B-49** | ~~**No routine or room can be created**~~ **RESOLVED 2026-09-02** — rooms in P0/A3 (`9af6a9d`, migration 065), routines in P0/A4 (`2e90d1b`, migration 069) | Nothing inserted into `routines` or `rooms` outside tests; **0 rooms and 0 routines across 112 tenants**. The solver, generation review, routine editor and exam-routine surfaces all sat on top of a row nothing created. `editor.ts` now has create-routine/place/assign/remove and `routine-editor-view.ts` drives them. This was also P9's largest prerequisite. | P9 | **HIGH** | **Yes** | **OPEN** | Owner SaaS audit §17, §18 |
| **B-50** | **Production schedules nothing** | **Re-verified 2026-09-08 (pre-pilot pass): the REPOSITORY half is done and the row's premise is stale.** `deploy/` now holds six files — `shikhon-{sms,maintenance,monitor}.{service,timer}` — plus `shikhon-cron.md` with the exact install commands. The timers are written in UTC on purpose (a host moved to Asia/Dhaka would otherwise shift both daily jobs six hours silently), `Persistent=true` on the dailies, not on the monitor. All three endpoints exist and accept `CRON_SECRET` through `matchServiceKey(… {allowCron:true})`. **What remains is installing them on the VPS, which needs host access this session does not have** — outbound SSH is blocked here, so nothing is claimed as installed. The monitor was exercised locally and correctly reports all three jobs as `minutesSinceSuccess: null` → “The {job} job has never run”, which is the honest state until an operator runs the four commands in `deploy/shikhon-cron.md`. The original wording follows: `deploy/` holds one systemd unit and it is a plain `Type=simple` web process — no timer, no `OnCalendar`. The only crons in the repo are in `vercel.json` (SMS dispatch, maintenance) and a Netlify bundle (monitor), and production is a VPS. So SMS dispatch, partition maintenance and the alert monitor are unscheduled on the host serving traffic. The backup cron IS installed on the box and is also absent from the repo. | — | **HIGH** | **Yes** | **EXTERNAL — repository side COMPLETE, host install NOT DONE** (2026-09-08). Blocks a pilot until an operator installs the timers. | Owner SaaS audit §12, §19; pre-pilot pass |
| **B-51** | **Five of seven alerts cannot fire on a total outage** | They are ratio/floor conditions. A deployment with zero sends, zero syncs, zero logins and a 4,000-device push fleet evaluates to an **empty alert list**. `sms_queue_stalled` requires `smsQueuedNow > 0` but the only producer of those rows is the worker that has stopped. `push_failure_rate` reads `push_subscriptions.last_failure_at`/`failure_count`, two columns **no code ever writes**. There is no heartbeat and no all-clear, so a silent sink and a healthy deployment are indistinguishable. | P-ops | **HIGH** | **Yes** | **RESOLVED 2026-09-06** (P-ops A+B `54610ce`, verified end to end in §3). Migration 071 added `ops_job_runs`; three non-ratio conditions (`job_never_ran`, `job_silent`, `job_failing`) read it through `app.job_run_status()`. §3 proves the whole loop against a real database, not just the rule: never-ran fires, a healthy schedule is silent on an otherwise-idle deployment, a stale heartbeat is caught against each job's OWN interval, a failing job names its error, and **recovery clears it** — the half that decides whether an operator trusts the alert. `shikhon_app` cannot forge a heartbeat (071 REVOKEs by name; the blanket GRANT in 010 would otherwise have handed it write access). **Structural limit, asserted rather than hidden**: the monitor cannot report its own death, because `job_silent` for the monitor is only evaluated BY the monitor — it fires on its first run back, and catching it during the outage needs an external check. 9 tests. | Owner SaaS audit §13 |
| **B-52** | **Three platform controls report success on a school that does not exist** | `POST /opsstate`, `/portal` and `/grace` returned **200** for a tenant with 0 rows in `tenants`, and wrote three audit entries for operations that never occurred. They `UPDATE … WHERE tenant_id = $1` with no `rowCount` check. The fix shape is already in the codebase: migration 053 gave `app.set_student_cap` `IF NOT FOUND THEN RAISE EXCEPTION` for exactly this, and its header describes the bug. | P-ops | **MEDIUM** | No | **RESOLVED 2026-09-06.** Five endpoints were fixed earlier via `requireExistingTenant`. P-ops §4 generalised the regression test from "the five the row named" to **every POST that takes a tenantId**, and found three more: **`/branding` answered 200** for a ghost (echoing the colour back and writing an audit row), while **`/admin` and `/payment` answered 500** — "we are broken" where the truth is "that school is not here", the exact mistake `/status` used to make. `/payment` is the one with money on it: an operator recording a bank transfer against a mistyped id got an engineer's problem instead of a corrected id. All three guarded; the test now also keeps a **bystander school** and asserts no mutation aimed at one school changes another or writes into its trail. 8 tests. | Owner SaaS audit §2 |
| **B-53** | **A disabled service's data is still served by un-gated siblings** | Disabling `attendance` refuses the gated endpoint, but `GET /academics/students/history` returned the identical record the gate had just refused. Same for finance: a principal reads the ledger from `/ops/dashboard` and a guardian the balance from `/academics/ward`, under both *limited* and *maintenance*. Disabling `push` does not stop push at all — only the subscribe endpoint is gated, not the sending path. | P-ops | **HIGH** | **Yes** | **RESOLVED 2026-09-03** — every sibling the row named, plus three the audit found. Reproduced first: a `starter` school (finance **`not_in_plan`**, never bought) was served `{"invoiced":"1300.00",…}` on `/ops/dashboard` and every invoice, balance and receipt id from `/academics/students/history`, while `/finance/invoices` refused the same session. Six sites fixed — `studenthistory`, `ward`, `hierarchy`, `dashboard`, `document`, `examroutine` — each block gated on its OWN service rather than the endpoint gated as a whole, because a school without finance still legitimately wants its attendance. **`/ops/document` was the worst**: branded, printable fee receipts, report cards, admit cards and attendance sheets were issued with their content service off — paper a school hands to a parent. The push half is B-83. 24 regression tests across three suites, each proven red first. | Owner SaaS audit §3 |
| **B-54** | **Tenant suspension does not revoke sessions** | M1 gave *user* deactivation session revocation; tenant suspension never got it. Restoring a school silently re-arms every 30-day refresh token and flushes months-old queued SMS to parents. Related: "read-only" mode in practice locks everyone out, because the auth endpoints are themselves gated — nobody can log in, refresh or log out. | P-ops | **MEDIUM** | No | **RESOLVED 2026-09-06 (P-ops §9).** *First half — not a defect.* Suspension needs no revocation sweep: every request opens through `withTenant`, which asks `app.tenant_access` on the connection it already holds, so a suspended school stops being honoured on the NEXT request rather than being listed somewhere. Verified against a live pre-suspension token: the read 403s with `tenant_blocked`, refresh is refused too (it rotates, so an open door there renews forever), reactivation restores both, and a bystander school is untouched. *Second half — real, and fixed.* A school in `limited` (arrears) could read but **could not refresh**, because rotating the token is a write inside `transaction_read_only = on`. Every user was signed out within one access-token lifetime with no way back in: read-only had become suspension by accident, making the softer state useless. `withTenant` gained `sessionWrite`, deliberately BELOW the `access = 'none'` check, so a suspended school still cannot renew. A `user_sessions` row is not the school's business data. 6 tests. | Owner SaaS audit §3, §9 |
| **B-55** | **Institution identity is SQL-only: rename, slug, type, contact** | No code anywhere writes `tenants.name_bn`, `name_en`, `slug`, `eiin`, `district`, `upazila`, `address_bn`, `stream` or `level` after creation. A school registered with a typo is stuck until an engineer opens psql. At 100 schools this is routine, not an edge case. | **Split — see classification** | **MEDIUM** | No | **CLASSIFIED 2026-09-06 (P-ops closure). Not one item but two, with different answers.** **(B) INTENTIONAL PLATFORM-ONLY — `slug`.** Migration 069's `assert_tenant_columns_platform_owned` trigger refuses it from `shikhon_app` by name, with the stated reason *"plan, cap, lifecycle, slug, weekend, shifts and key material are set by the platform, not by a school account"*. A slug is in the school's install link and `start_url`; changing it after launch moves every device's entry point. This is a design decision, not a gap, and needs no UI. **(A) REQUIRED PLATFORM-ADMIN UI — `name_bn`, `name_en`, `eiin`, `district`, `upazila`, `address_bn`.** No deliberate lock exists on these — they are absent from the 069 trigger's list and simply have no writer. They are ordinary operator corrections: a typo at registration, an EIIN issued after onboarding, a district fixed. **Sharpened by a finding**: the school's own branding screen writes a DISPLAY name into `settings->branding`, so a school's documents and app shell can show the correct name while `app.platform_overview()` and the operator console still show the typo. The gap therefore looks closed from inside the school and is not — which is worse than an obvious hole. **Not built here**: P-ops is operational hardening and a new console screen is feature work. Recorded per D13 as **"no writer — UI and endpoint both pending"**, never as complete, and documented in the runbook rather than left as undisclosed SQL-only behaviour. | Owner SaaS audit §2; classified P-ops closure |
| **B-56** | **064 let `PATCH /ops/guardians` resurrect a revoked guardianship** | Migration 064 repaired the conflict target but did not exclude revoked rows from the update path. Separately the office cannot tell a revoked link from a live one — `revoked_at` is not in the payload — and `/academics/roster` hands every staff role the student's own phone with no contact gate. | — | **MEDIUM** | No | **RESOLVED 2026-09-08 (pre-pilot pass)** · `84e9aaf`. Both halves. **Contact:** `CONTACT_ROLES`/`maySeeContact()` in server-core is now the single rule; `/academics/roster` gated the VALUE (a subject teacher still reads the roster and gets `null` where the number was), `students/history` uses the shared list instead of a local copy, and `/ops/guardians` keeps its stricter three-role gate — nothing was widened. **Revocation:** reproduced against PostgreSQL through the endpoint first (one revoked link in, 200 out, `[revoked, live]` with a new linkId), then refused with `link_revoked` (409); mutation-checked. Re-linking stays a deliberate POST. Already-correct properties pinned: `guardianship_hide_revoked` and SMS dispatch running as `system_ingest`. 14 tests. | Owner SaaS audit §16; pre-pilot pass |
| **B-57** | **The plan catalogue is largely inert** | `plans.is_active`, `plans.trial_days` and `plans.billing_cycle` are written by `POST /plans`, echoed to the console, audited — and **read by no runtime code at all**. Plan edits propagate inconsistently: `services` is live-joined (instant fleet-wide), `student_cap` is a copy that never propagates, `is_active` propagates nowhere. There is no invoice, renewal, reminder or dunning for the company's own fee, and nothing computes what a school **owes**. | — | **MEDIUM** | No | **OPEN** | Owner SaaS audit §3 |

| **B-58** | **An ops-svc suite failed once in ten runs and left nothing to read** | `services/ops-svc/test/branding.test.ts` reported as a WHOLE-FILE failure — the signature of a hook throwing rather than an assertion failing — during a full `test-all` run. It did not reproduce in nine subsequent runs (3 isolated, 2 paired after academics-svc, 4 via `npm test` exactly as the runner invokes it), and the runner’s 40-line tail had already scrolled past the cause. Rate limiting was checked and ruled out: the only depleted buckets were `otp_request`/`otp_verify`, and this suite makes about seven `brand` calls. **`test-all.mjs` now dumps full stdout+stderr to `test-failure-<workspace>.log` on any failure**, so the next occurrence is diagnosable. Not closed: an intermittent failure that was never explained is not a passing test. | P0 | MEDIUM | No | **OPEN — unreproduced, now instrumented** | P0 |
| **B-59** | **The service worker caches `/api/v1/rms/` and `/api/v1/academics/` as reference data, which is wrong for an authoring register** | Both prefixes are served stale-while-revalidate ("instant render beats freshness here"). Correct for a published timetable; wrong for any register read immediately before a write and re-read after it. Observed in a browser: creating a room succeeded, the success line said so, and the list underneath still showed the empty state. `/rms/rooms` and `/academics/exams` are now network-only (`7f9c7be`). **The rest of both prefixes has not been re-examined** — every future authoring endpoint under them inherits the bug by default, because the rule is a prefix match rather than anything that knows what a screen does. | P0 | MEDIUM | No | **PARTIALLY RESOLVED** | P0 |
| **B-60** | **Both P0 authoring screens refused saves in total silence** | A refused write returned a 409 and a Bangla sentence; the drawer closed, the typed values were discarded, the list was re-read and nothing was shown — a form vanishing and an unchanged list reads as "saved". Two independent faults, either sufficient: `handle.close()` ran *before* the request was sent, and `send()` set `this.error` whose own `finally` called `load()`, whose first statement is `this.error = ''`. `rooms-view.ts` shipped with both at `9af6a9d`, so every failed room save had been silent since. Both now show the server's own sentence inside the drawer, announce it, keep the typed values, and close only on acceptance. **The wider risk stands**: the same optimistic `handle.close()` appears in `structure-edit.ts` (which handles it correctly) and the pattern is what the next writer will be copied from. | P0 | HIGH | No | **RESOLVED 2026-09-02** (`31d364d`), regression test `apps/pwa/test/writer-save-errors.test.ts` | P0/A2 |
| **B-61** | **`services/finance-svc` had test files and no `test` script** | Its twelve A2 tests had never once run under `npm test`. `scripts/test-all.mjs` reported it as an ERROR rather than a tick, which is that check working exactly as designed — but nothing prevents the next new service from being created the same way, and the error is only visible to someone reading the tail of a full run. | P0 | MEDIUM | No | **RESOLVED 2026-09-02** (`577162e`) — a guard that fails the suite on a workspace with tests and no script is not yet written | P0/A2 |
| **B-62** | **Neither P0 view could be imported by a test at all** | `rooms-view.ts` and `fee-structures-view.ts` both used a `private readonly o` parameter property, which Node's type-stripping runner rejects outright with `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`. That is why neither had a view test — the absence looked like a gap in diligence and was actually an impossibility. Every other view uses explicit assignment. **Nothing enforces this**: the next view written with a parameter property is silently untestable again, and typecheck passes. | P0 | MEDIUM | No | **RESOLVED 2026-09-02** for both views (`31d364d`); no guard exists | P0/A2 |
| **B-63** | **`db/tests/guardian_links.sql` is not idempotent** | It commits its fixture tenants and only rolls back its last block, so a second run against the same database fails on `tenants_pkey`. Pre-existing and unrelated to migration 067; it passes once its own leftovers are cleared, which is how 26/26 was reached. Related to B-35, but distinct: B-35 is concurrent runs racing, this is a single suite that cannot be run twice in sequence. | P0 | LOW | No | **OPEN** | P0/A2 |
| **B-64** | **`platform-svc` and 41 `server-core` tests skip for want of a credential** | They need `PLATFORM_DATABASE_URL` in addition to `DATABASE_URL`. With only the latter set the runner prints `0 tests — NOTHING RAN (2 suite(s) skipped)`, which is honest, but the default local run therefore misses 88 tests — 1,572 rather than 1,660 — and the gap is easy to read as "that service has no tests". | P0 | LOW | No | **OPEN** — documented here; the runner's message could name the specific variable | P0/A2 |
| **B-65** | **`psql` is not on this machine's PATH, so the 26 SQL suites are skipped by default** | PostgreSQL runs in Docker (`shikhon-r5`); the host has no client. `scripts/test-all.mjs` correctly prints `NOTHING RAN`, but the practical effect is that the SQL invariants — the ones that check the schema a migration just changed — do not run in the ordinary local loop. They were run for this checkpoint via `docker exec … psql`, 26/26. The runner could fall back to a container when one is detected. | P0 | MEDIUM | No | **OPEN** | P0/A2 |
| **B-66** | **B-58's signature, now observed in THREE different files** | Whole-file failures with **empty stderr** — `test at test\<file>:1:1`, `'test failed'`, no assertion named — which is a hook throwing or a process dying, not a test failing. Instances: `ops-svc/branding.test.ts` (P0 checkpoint 3, the original B-58), `platform-svc/nonexistent-tenant.test.ts` and `ops-svc/events.test.ts` (both P0/A1, two different full-suite runs). Each failed FAST (317ms, 362ms) — too fast to be a lock wait — and none reproduces: 6/6 clean runs of ops-svc alone, 4/4 of platform-svc alone, 6/6 in isolation, and 2 of 4 full-suite runs green at 1,675. **Ruled out with evidence:** connection exhaustion (max_connections 100, 6 in use), cross-workspace parallelism (`test-all.mjs` runs workspaces sequentially via `execSync`), and `installTestKeys` (process-local `process.env` only). **Narrowed suspect, NOT proven:** all three files share the identical hook shape `installTestKeys → lockFixtures → createDb`, and `lockFixtures` deliberately **unrefs its socket** so a suite that dies badly still releases the lock. If at some moment no ref'd handle remains, Node exits the process mid-file — which would produce exactly a fast, silent, whole-file failure. Testing that means instrumenting process exit, not rerunning. | P0 | HIGH | No | **OPEN — narrowed suspect ACTED ON 2026-09-08 (pre-pilot pass), not yet proven.** Two things changed. (1) The runner now fails loudly and by NAME: `test-all.mjs` tracks every workspace it STARTS and every one that reports a count, prints `13/13`, and exits non-zero listing any that started and never reported — negative-tested by making a workspace die silently, which produced `12/13` and named `services/sync-svc`. Before this, a lost workspace could only be identified by subtracting two runs' totals by hand. (2) The narrowed suspect was acted on: `lockFixtures` no longer leaves its socket unref'd. The socket is REF'd while a suite runs — so Node cannot exit mid-file when it is the only handle left, which is the hypothesised mechanism — and an UNREF'D 5-minute watchdog releases the lock if a suite wedges, preserving what the unref was protecting. **Not claimed as fixed:** this flake never reproduced on demand, so four consecutive clean 13/13 runs (against a prior 2-in-5 failure rate) is suggestive and not proof. B-58 stays open with it. | **CLOSED 2026-09-10 — mechanism proven, fix landed, and verified on the real system runtime. It was never this repository's code.** A TCP socket opened inside a `node --test` PER-FILE CHILD PROCESS intermittently aborts that child on **Node 24 before 24.21.0, on Windows**. The child dies with Windows status `0xC0000409` (an `abort()`) 220-450 ms in, before writing a byte — so the runner reports the whole file at line 1:1 with `'test failed'`, no assertion, empty stderr. **Proven by a ladder, one variable at a time:** 20,900 child processes across rungs that open no socket (synthetic TS/JS, heavy type-stripping, the project module graph, jose keygen, a constructed pg pool) crashed **zero** times; adding a connection crashed at 3%. **The advisory lock is not involved** — v6a removes it and fails *more* (9/300), which finally kills the `lockFixtures` unref theory the pre-pilot pass acted on. Narrowed until nothing of ours remained: raw `net.connect()` with no `pg` and no project imports reproduces (8/300); a socket to a throwaway server **inside the child**, no database anywhere, reproduces (3/300); a **UDP** socket does not (0/300); `--test-isolation=none` does not (0/300); the identical socket work as a plain `node file.mjs` does not (0/3,300). **Concurrency is NOT the cause** — it still happens at `--test-concurrency=1` and the per-child rate is flat (0.18-0.27%); the earlier “0 failures at ≤ 8” was a small-sample artifact, and a concurrency cap would have turned the suite green while fixing nothing. **The proof is one variable:** same machine, same session, same fixture — Node **v24.15.0 → 11 crashes / 500 runs (5,500 children)**, Node **v24.21.0 → 0 / 500** (Fisher p ~ 0.0005). Node 22.23.2, which CI pins, is clean at **0/500**, which is why only the DB-backed suites were ever hit (they alone open sockets) and why CI never saw it. **Fix:** `scripts/test-all.mjs` refuses a Node 24 build below 24.21.0 with the evidence attached, and names a crashed child for what it is — conditional on the runtime, so a patched Node is told B-66 is NOT the explanation. `engines` stays `>=22` so green CI on Node 22 is not invalidated. No retries, no concurrency cap, no test weakened, no application code touched. **Validated:** 1,000 reproducer runs (11,000 children) and **10/10 consecutive 13-workspace suites** on 24.21.0, 2,270 tests, total stable. **B-58 is closed with it** — same mechanism, same signature. **Closure evidence, on the INSTALLED runtime** (v24.21.0, `C:\Program Files\nodejs\node.exe`, MSI SHA-256 verified against the release `SHASUMS256.txt`): the minimal TCP + `node --test` reproducer **0 crashes / 500 runs (5,500 children)** where the same fixture crashed **11 / 500** before the upgrade; the real `academics-svc` suite **185 tests, 0 fail**; the guard allows 24.21.0 without refusing; the crashed-child detector still names a deliberately aborted child and correctly reports that B-66 is NOT the explanation on a patched runtime. Plus, on the verified portable build of the same version: 1,000 reproducer runs (11,000 children) clean and **10/10 consecutive 13-workspace suites** at 2,270 tests with the total stable. **B-58 closes with this** — same mechanism, same signature, first seen in `ops-svc/branding.test.ts` at P0 checkpoint 3. | P0/A1 |
onexistent-tenant.test.ts:1:1`, `'test failed'`, **empty stderr** — which is a hook throwing rather than an assertion failing, exactly what B-58 records for `ops-svc/branding.test.ts`. Did not reproduce: 6/6 in isolation, 4/4 running that workspace exactly as `test-all.mjs` invokes it, and the next full suite 1,675/1,675. Ruled out: connection exhaustion (max_connections 100, 6 in use) and cross-workspace parallelism (`test-all.mjs` runs workspaces sequentially via `execSync`). The remaining suspect is `lockFixtures`' **single global advisory lock**, which every DB suite in the repo shares and which `node --test` makes several processes queue on within one workspace. Related to B-35 (fixed-uuid fixtures) but distinct: this is the LOCK, not the ids. | P0 | HIGH | No | **OPEN — B-58 is NOT closed by this; it now has two instances** | P0/A1 |
| **B-67** | **`exam_marks` and `exam_results` writes were governed by tenant-match alone** | Migration 066 scoped `exams` and `exam_subjects` and stopped there: `exam_marks` had a RESTRICTIVE policy on INSERT and SELECT only, `exam_results` on SELECT only. Proved live as `dept_head` — a role `marks_write_scope` deliberately excludes from entering a mark — `DELETE FROM exam_marks` returned **1 row on an exam whose results were PUBLISHED**, and `DELETE FROM exam_results` returned 1 row. `trg_marks_immutable` is `BEFORE UPDATE OF` four component columns and never fires on DELETE. `subjects` and `academic_years` also had no DELETE policy while `classes` and `sections` did, and both cascade into the exam tables. | P0 | HIGH | No | **RESOLVED 2026-09-02** (`62b6146`, migration 068) — five policies added, publish path re-verified | P0/A1 |
| **B-68** | **Publishing an exam ROUTINE wrote the RESULTS status, bricking the exam** | `rms-svc/api/examroutine.ts` set `exams.status = 'published'` to mean "the timetable is out". `appliers.ts`, `publish.ts` and `exams.ts` all read that value as "a parent has been shown a grade", so from that moment every mark was refused `published_marks_immutable`, results publication was refused `already_published`, and the exam could not be corrected. Nothing moves status backwards. **Four tests asserted the conflation**, which is why it survived a green suite. | P0 | HIGH | No | **RESOLVED 2026-09-02** (`62b6146`) — `exams.routine_published_at`, both routine guards re-pointed, corrupted rows repaired, and the four tests inverted into guards | P0/A1 |
| **B-69** | **`demo.ts`'s exams case was method-blind and shape-blind** | One `case '/api/v1/academics/exams'` answered GET, POST and PATCH alike with the per-section marks feed. A create screen would have reported success in the **public** `/demo` preview while creating nothing, and the year-register read would have rendered rows with every field missing. **The wider risk stands**: the demo dispatcher keys on `url.pathname` and only some cases inspect `init.method`, so the next write endpoint added to `/demo` inherits the same fake success by default. No guard exists. | P0 | MEDIUM | No | **RESOLVED for exams 2026-09-02** (`660afed`); the pattern is unguarded | P0/A1 |
| **B-70** | **Ten further exam-domain defects found during the A1 audit and NOT fixed** | Recorded so they are not rediscovered: (a) `publish.ts` returns **500 where it means 409** — it has no SQLSTATE 23514 branch, so a routine clash or unstaffed hall surfaces to the office as an opaque server error; (b) `examroutine.ts`, `scripts.ts`, `hierarchy.ts` and `ops-svc/dashboard.ts` build their context with **no `service:` key**, bypassing the entitlement gate entirely; (c) PATCH's `ExamBody` declares `academicYearId` and `termId` and the UPDATE sets neither — a client PATCHing them gets 200 and no change; (d) `uq_exam_name_year` deduplicates `name_bn` only, so the English-medium rendering can still show two identical rows; (e) paper generation joins `class_subjects` on the class and ignores per-student subject choice (`student_subjects`), inflating `paperCount` for split sections; (f) `publish.ts` pins a multi-section student to `MIN(es.section_id::text)` which then drives `rank_in_section`; (g) exam-list reads are `requireStaff`, so `accountant` and `librarian` see every paper's component maxima and mark counts; (h) the authoring role set (4 roles) and the marking role set disagree, so `school_owner` and `dept_head` can create an exam whose marks they cannot enter; (i) the status transition graph is unvalidated — `published → planned → locked` was demoted and jumped with neither guard firing; (j) `/api/v1/rms/examroutine` has no `/demo` case at all, so that screen is permanently broken in the public preview. | P0 | MEDIUM | No | **OPEN** — (a), (b) and (j) are the ones a pilot would notice first | P0/A1 |
| **B-71** | **Any account in a school could upgrade its own commercial plan** | `tenants` carried exactly one policy — `tenant_self`, PERMISSIVE FOR ALL, `id = app.current_tenant()` — with no role predicate. Proved live as `shikhon_app` with `app.role='student'`: `UPDATE tenants SET plan_code='complete', student_cap=999999` returned 1 row. `app.tenant_access()` reads `plan_code`, so the entire D16 commercial layer P7 built was bypassable by a student account. The same policy also let any account rewrite `weekend_days`, i.e. declare the school has no teaching days. RLS cannot express per-column rules, so the fix is a BEFORE UPDATE trigger refusing the platform-owned columns from the application role; SECURITY DEFINER functions and the platform's own database role are unaffected, and ops-svc's branding/settings writers touch only `tenants.settings`. | P0 | HIGH | No | **RESOLVED 2026-09-02** (`c9bfa70`, migration 069) | P0/A4 |
| **B-72** | **The routine tables' DELETE was open to everyone** | `routines`, `routine_slots` and `routine_substitutions` carried RESTRICTIVE policies written `FOR ALL … USING (true)`, which gates INSERT and an UPDATE's new row and gates DELETE not at all — so a student could delete the school's timetable (deleting the `routines` row cascades to every slot). `period_templates`, `period_definitions` and `routine_slot_sections` had no write scope of any kind, so any account could rewrite when the school day starts and ends. Same shape as 066 on `exams` and 068 on `exam_marks`. | P0 | HIGH | No | **RESOLVED 2026-09-02** (`c9bfa70`, migration 069) | P0/A4 |
| **B-73** | **`explainConflict` had never once produced its sentence** | §8.1 requires a routine clash to name the conflicting assignment rather than say "invalid". `explainConflict` runs a SELECT *after* the failed write to find it — but a failed statement aborts the PostgreSQL transaction, so that query died with 25P02 and the 409 became a 500. Every real clash on the `move` path, since the routine editor shipped, came back opaque. Fixed with a savepoint. **The wider risk stands**: any handler that queries after a caught constraint violation has the same bug, and nothing checks for it. | P0 | MEDIUM | No | **RESOLVED for editor.ts 2026-09-02** (`2e90d1b`); the pattern is unguarded elsewhere | P0/A4 |
| **B-74** | **Teacher and room clashes are unenforced while a routine is a draft** | `rs_no_teacher_double_booking` and `rs_no_room_double_booking` carry `WHERE routine_status = 'active'`; only `rs_no_section_double_booking` fires at every status. So the database permits a draft with one teacher in two places and refuses it all at once at publish, when `propagate_routine_status` flips every slot. A4 checks those two dimensions in the API at placement time so the coordinator is told immediately, but that is application-level and races are still resolved at publish. Whether the constraints should instead be unconditional is a domain decision with a real cost — a half-built draft would become unbuildable. | P0 | MEDIUM | No | **OPEN — documented, mitigated in the API, not changed in the schema** | P0/A4 |
| **B-75** | **`period_template_defaults` has no rows for the `evening` or `single` shifts** | `app.provision_tenant` clones a bell schedule per shift in `tenants.shifts` from that global table, which holds only `day` (10) and `morning` (9) rows; migration 012 maps `single`→`day`, and nothing maps `evening`. A tenant provisioned with an evening shift gets a named but EMPTY `period_templates` row — the INSERT…SELECT matches nothing and raises no error. A4 refuses routine creation with `no_period_template` and says so in Bangla, but the underlying seed gap remains, and there is no UI to author a period template. | P0 | MEDIUM | No | **OPEN** — A4 surfaces it; the seed and a template editor are unbuilt | P0/A4 |
| **B-76** | **The routine domain is not an entitleable service** | The service catalogue (migration 051) has no `routine` or `timetable` entry, so rms-svc handlers building their context without a `service:` key are not bypassing a gate — there is nothing to consult. This is **distinct from B-70(b)**, which concerns handlers omitting a service that does exist (`examroutine.ts` and `scripts.ts` should pass `results`). Whether the timetable becomes a purchasable service is a commercial decision (D16), not an engineering one. | P0 | LOW | No | **OPEN — needs an owner decision** | P0/A4 |
| **B-77** | **`payment_receipts` and `invoice_lines` writes were governed by tenant-match alone** | `invoices` had four per-command policies and a SELECT scope; the tables either side of it had only the PERMISSIVE `tenant_isolation`. Proved live as `app.role='student'`: INSERT a receipt for ৳99,999 (allowed), UPDATE a receipt's amount (1 row), DELETE a receipt (1 row), UPDATE `invoice_lines` to zero what is owed (2 rows) — while `UPDATE invoices` correctly returned 0. SELECT was unscoped too, so every student could read every family's payment history. | P-writers | HIGH | No | **RESOLVED 2026-09-03** (`ddbab64`, migration 070) | B-48 audit |
| **B-78** | **A concurrent payment could be applied with no receipt** | The webhook built `receipt_no` as `max()+1` inline and wrote it `ON CONFLICT (tenant_id, receipt_no) DO NOTHING RETURNING receipt_no`, reading the result as `rows[0]?.receipt_no ?? null`. Two concurrent transactions compute the same number; the loser got zero rows and nothing checked. `app.apply_payment_to_invoice` had already run, so the money was applied, the invoice marked paid and the ledger posted with no receipt and no error. | P-writers | HIGH | No | **RESOLVED 2026-09-03** (`ddbab64`) — `app.next_receipt_no()` under an advisory lock, no `ON CONFLICT`, explicit throw where the swallow was | B-48 audit |
| **B-79** | **A test fixture wrote UTC dates while the product reads Dhaka** | `academics-svc/test/ward.test.ts` used `CURRENT_DATE`; the endpoint reads `app.today_dhaka()` (migration 059). They agree for eighteen hours a day and differ for the other six, so the suite **failed every evening UTC and passed every morning** — indistinguishable from a flake. Caught at 20:39 UTC on the 2nd, which is 02:39 on the 3rd in Dhaka. **The wider risk stands**: no guard stops another fixture reintroducing `CURRENT_DATE`, and migration 059 touched 31 embedded-SQL sites for the same reason. | P-writers | MEDIUM | No | **RESOLVED for ward.test.ts 2026-09-03** (`ea01193`); the pattern is unguarded | B-48 gate |
| **B-80** | **`invoices.late_fee` has no writer, and `invoice_no` is generated with `count(*)`** | `fee_structures.late_fee_per_day` and `late_fee_cap` are configurable and read, but nothing ever writes `invoices.late_fee` — a school that configures a late fee never charges one. Separately `invoice_no` is built from `(SELECT count(*) … WHERE billing_period = $2)` plus `row_number()`, the same non-atomic shape B-78 fixed for receipts. | **A finance phase, not P-ops** | MEDIUM | No | **OPEN — classified 2026-09-06 (P-ops §12), deliberately NOT taken into P-ops.** Neither half is an operational defect and neither is data corruption. *Late fee*: `late_fee` is read by `finance-svc/api/index.ts` and written by nothing, so the column is always 0 — the invoice is internally consistent and simply under-bills. A school that relies on late fees loses that revenue silently, which is a **missing feature**, not a broken one. *Numbering*: `invoices` carries `UNIQUE (tenant_id, invoice_no)`, so a reused number RAISES 23505 rather than producing two invoices with one number — a generation run can fail after a cancellation, with a clear error, and no book is ever wrong. **Does it block a real institution's finance lifecycle?** No: a school can bill, collect, receipt and post to the ledger today (B-48/B-77/B-78 closed those). It blocks *late-fee* billing only. Depends on a product decision about when a late fee accrues (per day from `due_on`? capped by `late_fee_cap`? recomputed on payment?) — which is why it is a phase and not a patch. | B-48 audit |
| **B-81** | **`app.seed_chart_of_accounts` is defined and never wired** | Migration 016 defines it and the MFS chart, and nothing calls it. A tenant whose chart was never seeded takes payments that produce a receipt and **no ledger rows**. | — | MEDIUM | No | **MATERIALLY CORRECTED 2026-09-06 (P-ops §E).** The claim is wrong as written: `app.provision_tenant` **inlines** the chart of accounts — 15 `ledger_accounts` rows at migration 012 §8 — and the fee heads at §7. A provisioned school has both, proved by the fresh-tenant walk. The dead `seed_chart_of_accounts` function is a tidiness matter, not a money one. **The real, narrower gap**: a school created through `POST /tenants` and never provisioned has neither, and no endpoint anywhere writes `ledger_accounts` or `fee_heads`, so the only route to them is `/provision` or psql. Not blocking a normal onboarding — provisioning is a standard step and the §E walk performs it — and recorded rather than closed, because giving an unprovisioned school a chart is a product decision about what `/tenants` alone should mean. | B-48 audit; corrected by P-ops §E |
| **B-82** | **The class-performance screen 500'd on every request it had ever received** | `classperf.ts`'s `loadChoices` and `loadHeader` selected `s.name_bn` from `sections`, whose column is `name` — `subjects` is the table with `name_bn`, and it is aliased `sub` two lines away. SQLSTATE 42703 at parse time, so the failure needed no data and was not conditional: the endpoint answered `{"error":"internal_error"}` from the day it was written and the screen behind it has never once rendered. Found by an entitlement probe that was not looking for it, because the handler's `catch` logged the cause where the response did not. | P-ops | **HIGH** | No | **RESOLVED 2026-09-03** — three occurrences corrected; a smoke test now asserts a 200 and a `choices` array, which is enough because the failure was at parse time | P-ops §5 audit |
| **B-83** | **No school on the pilot plan has ever received a push notification** | `SmsDispatchWorker.run` opened its transaction with `service: 'sms'`, so `tenant_guard` refused the whole run for any school whose SMS was off — and the push stage runs *inside* that transaction. `pilot` and `madrasa_basic` carry `push: true` and **no `sms` key at all**, so every school on them had `sms = not_in_plan`, the run was refused before its first statement, and push never executed. Verified across four live pilot tenants, each showing `push_state = enabled`. Nothing reported it: the subscribe endpoint answered 200, devices registered, and a refused run reports a blocked tenant rather than a missing feature. `pilot` is the plan real pilot schools are on. | P-ops | **HIGH** | No | **RESOLVED 2026-09-03** — the run is gated on tenant ACCESS only; each transport is gated on its own service inside, so a suspended school still sends nothing while a push-only school pushes. Seven tests, five proven red — including one on a school **genuinely on the `pilot` plan**, where sms is `not_in_plan` rather than a simulated `disabled`, because `tenant_service_state` has separate branches for those two and "both fail the same check today" is the assumption a plan-shaped bug hides behind | P-ops §5 audit |
| **B-84** | **An entitlement refusal is worded as a permission refusal** | `serverMessage` drops the server's words on any 403 — correct for a role refusal, wrong for a gate one. `HttpError`'s own comment already drew the line it needed: "a refused ROLE needs a different person, a blocked TENANT needs a payment or a call to us — and the screen has to say which." It did not say which, so a guardian at a school with no finance module read "…দেখার অনুমতি আপনার নেই। প্রয়োজন হলে প্রধান শিক্ষকের সাথে যোগাযোগ করুন" and was sent to a head teacher who would tell them the school does not use that part. | P-ops | MEDIUM | No | **RESOLVED 2026-09-06 (P-ops §4).** Four refusals now carry four sentences. *Server*: on a gate refusal that named a service, `app.tenant_service_state` rides along as `serviceState`, so `not_in_plan` (buy it) is machine-distinguishable from `disabled`/`maintenance` (wait) — one extra query on the refusal path only. *Client*: `refuseUnlessOk` became async and, on a 403, reads the server's code and Bangla sentence into `HttpStatus`; `deniedMessage`/`deniedContact` pick the wording and decide whether to offer a colleague at all — an arrears block has nobody to ask, and offering one wastes a trip. Wired into the six screens that refuse through `isDenied`. **Browser-verified**: the fees screen now reads "ফি ও হিসাব এই প্রতিষ্ঠানের প্ল্যানে নেই। প্ল্যান পরিবর্তনের জন্য shikhonBD-এর সঙ্গে যোগাযোগ করুন।" with no head-teacher line. 6 tests. | P-ops §5 browser check |
| **B-85** | **A push-only school has nothing to push** | Falls out of B-83's fix and is a product decision, not a defect to sneak in with it. Enqueueing a row into `sms_outbox` is the billable act, so with SMS off nothing is queued — and the push stage delivers *from that queue*. A school on `pilot` therefore now pushes correctly and pushes nothing, because push here is a cheaper transport for SMS traffic rather than an independent channel. Giving a push-only school its own notification queue is real design work: what is pushed, at what volume, with what dedupe and what daily bound, and whether an absence notice with no SMS behind it should still respect the SMS cap. | — | MEDIUM | No | **OPEN**, recorded rather than half-built | P-ops §5 audit |
| **B-86** | **A 500 on the student-history endpoint left no trace anywhere** | Its `catch` mapped any non-`HttpError` to `internal_error` and returned it, logging nothing — so the only way to diagnose one was to reproduce it by hand. Fixed in passing during the B-53 work (`console.error` on the server, response deliberately unchanged, since this endpoint returns a child's record and a leaked SQL message names columns). It paid for itself within the hour by naming the cause of B-82. **The pattern is unaudited**: no guard stops another handler swallowing an error the same way, and `classperf` only had a log because someone had written one by hand. | — | LOW | No | **RESOLVED for studenthistory 2026-09-03**; the pattern is unguarded | P-ops §5 audit |
| **B-87** | **A school could not add its first teacher without a 500** | `staff_profiles.employee_code` is NOT NULL and UNIQUE per tenant; `ops-svc/api/users.ts` passed `(body.employeeCode ?? '').trim() || null` into it, and the form marked the field **optional**. A principal who left the staff-ID box empty — the default path for any school that does not number its staff — got `{"error":"internal_error"}` while doing one of the first things a new school does. Invisible to every unit test because they all supply the field; found by the §E onboarding walk on its first attempt. | P-ops | **HIGH** | No | **RESOLVED 2026-09-06 (P-ops closure).** **Decision: REQUIRED, not generated** — and the product had already made it twice, differently, on purpose. `student_code` is GENERATED (`studentCodeFor`, `import-run.ts`) because a child does not arrive holding a student number. `employee_code` is SUPPLIED: `teacher-import.ts` refuses a CSV with no `employee_code` column (L121), fails any row whose cell is blank (L159), and de-duplicates within the file. It is the school's own staff number, already on their paperwork — generating one here would give the same teacher one code typed into the form and a different one imported from the school's spreadsheet. **A second 500 was found and closed with it, and it is the likelier of the two**: `ON CONFLICT (user_id)` covers the primary key and NOT `UNIQUE (tenant_id, employee_code)`, so re-adding a teacher or reusing a number raised 23505 → 500. Now 409 naming the field, with the whole handler in one transaction so no orphaned `users` row survives a refusal. The dead `isStaffRole` branch was removed: `GRANTABLE` holds no student or guardian. 5 tests; **browser-verified** — created a teacher, then collided with the same ID and read "এই কর্মচারী আইডি (EMP-777) আগেই ব্যবহার করা হয়েছে" with the form still open. | P-ops §E |
| **B-88** | **26 SQL suites had not run on any developer machine** | `scripts/test-all.mjs` required `psql` on PATH and reported `0 of 26 — NOTHING RAN`, honestly and uselessly: these are the only tests that exercise RLS, the RESTRICTIVE write scopes, the EXCLUDE constraints and the SECURITY DEFINER functions as PostgreSQL actually enforces them. The database this project develops against is a Docker container **that has psql inside it** — the client was never missing, only the PATH. Cost, measured on the day it was fixed: `schema_lint.sql` had been failing since P-ops A because `ops_job_runs` (migration 071) is not in its exempt list, two commits earlier, and nothing said so. The lint's own comment records the same thing happening to `plans` and `service_catalogue` in P7. | P-ops | **HIGH** | No | **RESOLVED 2026-09-06** — `scripts/sql-tests.mjs` pipes each file into the container's psql (ON_ERROR_STOP kept, a raising suite still fails the run, and a file using `\i`/`\copy` is refused rather than run with an unresolvable path); `test-all.mjs` falls back to it only when there is no local psql, so CI keeps its direct path. **26/26 executed, and 52/52 across two consecutive runs.** | P-ops §5 |
| **B-89** | **A student could name themselves the teacher of any section** | `section_subject_teachers` carried ONE policy — `tenant_isolation`, PERMISSIVE, FOR ALL — so tenant-match was the whole test. Proved live as `app.role='student'`: `INSERT … teacher_id = <own id>` → `INSERT 0 1`, and `DELETE FROM section_subject_teachers` → `DELETE 1`. Load-bearing rather than theoretical: `solve.ts:loadDemand` reads this table **and nothing else** to decide what the timetable must contain, so whoever writes it writes the school's entire teaching load. Same shape as B-77 — a table beside well-scoped neighbours inheriting only the isolation every table gets; `class_teacher_assignments` next to it got per-command scopes in migration 010 and this one was missed. | **P9-0** | **HIGH** | No | **RESOLVED 2026-09-06** — migration 072: INSERT/UPDATE limited to the three roles that may author a routine, DELETE refused to everyone (`USING (false)`) because this is a history table and erasing a row erases the fact that somebody taught a class. `dept_head` deliberately excluded: the table has no department column to scope them by, so admitting them would admit them to every subject in the school. 7 tests. | P9-0 inventory |
| **B-90** | **The solver read CLOSED teaching assignments as live demand** | `loadDemand` filtered on academic year and shift and **not** on `ended_on IS NULL`. `section_subject_teachers` is a history table, so every teacher who had ever held a subject was still returned — and the solver would place `periods_per_week` once per historical teacher. A school in its third year of using the product would get three sets of Bangla periods, one per reassignment. | **P9-0** | **HIGH** | No | **RESOLVED 2026-09-06** — one predicate. **Why it had never fired**: nothing in this repository had ever produced a closed row. Every fixture DELETEd instead of closing, so the history table had no history, and the missing filter could not be wrong. Migration 072's DELETE ban forced the fixtures to close rows — and the bug surfaced within minutes. A guard that makes a latent defect reachable is doing its job. | P9-0 inventory |
| **B-91** | **A table gets per-command scopes only when someone builds a writer for it** | Four instances now, all the same sentence about a different table: B-53 (composite endpoints), B-77 (`payment_receipts`, `invoice_lines`), B-89 (`section_subject_teachers`), B-92 (`class_subjects`, `teacher_availability`). The mechanism is consistent — a table is created with `tenant_isolation`, and per-command RESTRICTIVE scopes are added when a phase finally writes to it. So the tables NOBODY could write were never examined, which means the ones most in need of scoping were exactly the ones that did not get it. Each was found by a phase tripping over it, not by looking. | — | **HIGH** | No | **OPEN** — needs a sweep: every `public` table with `tenant_isolation` and no per-command policy, triaged into "needs scopes", "read-only reference data", or "deliberately open". `schema_lint.sql` is where the guard belongs once the list is triaged, so a new table cannot ship with isolation alone. | P9-2 |
| **B-92** | **A student could rewrite the curriculum and block any teacher** | `class_subjects` and `teacher_availability` carried `tenant_isolation` and nothing else. Proved live as `app.role='student'`: `UPDATE class_subjects SET periods_per_week = 20` → **`UPDATE 12`**, twelve rows of a real school's curriculum; and an INSERT marking a teacher unavailable 08:00–16:00 → **`INSERT 0 1`**. Between them these two tables decide how big the timetable is and where it may not go — `solve.ts` computes the week from `periods_per_week` and refuses any slot `teacher_availability` calls blocked. A student who set every subject to 20 periods a week would make the school's timetable unsolvable, and the failure would look like the solver's fault. | **P9-2** | **HIGH** | No | **RESOLVED 2026-09-06** — migration 073. Curriculum limited to the three routine-authoring roles; availability adds `dept_head` (every row names one teacher, so no school-wide blast radius, and a head of department is who knows about a Thursday clinic). DELETE allowed to the same roles on both, unlike 072's history table: removing a subject from a class is a real thing a school does in August, and a mistaken availability block must be removable the moment it is noticed. 15 tests. | P9-2 |
| **B-93** | **A bell schedule could contain overlapping periods** | `period_definitions` had `UNIQUE (tenant, template, period_no)` and `CHECK (ends_at > starts_at)` and **nothing** stopping period 3 running 10:00–11:00 while period 4 ran 10:30–11:30. The solver books teachers and rooms by TIME INTERVAL (F-506, cross-shift), so two overlapping definitions in one template are mutually exclusive for every teacher — an unfillable timetable produced by a bell schedule that looked fine. A4's own fixture relied on the gap, creating deliberately overlapping periods "so a clash is reachable". | **P9-2** | MEDIUM | No | **RESOLVED 2026-09-06** — `pd_no_overlap_within_template`, a GiST EXCLUDE matching the three that already protect `routine_slots`, partitioned by template so a morning and a day shift may still overlap each other (which is what cross-shift booking is for). The A4 fixture now uses a real consecutive schedule and reaches its three clashes in the SAME period, which is the real-world shape of each. | P9-2 |
| **B-94** | **In a two-shift school the second shift could not see the first, and the summary said zero conflicts** | `solve.ts` books teachers and rooms against every ACTIVE routine in the year (F-506) — correct while the only way to have a second shift was to publish it first. P9-3 creates a draft for every shift in ONE action, so the day shift was blind to the morning shift. The database did not object because `rs_no_teacher_double_booking` and `rs_no_room_double_booking` are predicated on `routine_status = 'active'`; that predicate is deliberate (two rival drafts of one shift must not block each other) and `editor.ts:findClash` already compensates for the same gap in manual authoring. And `generate.ts` reported `hardConflicts` as a hard-coded `0`. Found by `scripts/routine-benchmark.mjs` on its first full run: **40 stored room double-bookings** in an 80-section school, 4 in a 120-section college, every one at the shift handover, with a summary that said the routine was clean. Publishing either would have been refused by the database with an opaque error. | **P9-3** | **HIGH** | No | **RESOLVED 2026-09-06** — three fixes, one per cause. `solve()` gained `opts.alsoBookedAgainst` naming the sibling drafts of this run (a parameter, not "every draft in the year": a year accumulates abandoned drafts and booking against all of them would have the solver believe the school is full). `generate.ts` COUNTS hard conflicts from the stored rows and a non-zero count outranks everything else in the verdict sentence. A test plants a real conflict in a draft and requires the counter to find it. Separately, `shiftsOf` ordered by the shift's TEXT so `day` was solved before `morning` and won every contended room — it orders by the enum, which is declared in clock order. | P9-3 |
| **B-95** | **The routine authoring screens were served from the service worker's cache** | `/api/v1/rms/` as a whole is `stale-while-revalidate` — right for a published timetable a teacher reads in a corridor, wrong for a register read immediately before a write and re-read after it. P0 had already carved out `/rms/rooms` and `/rms/editor` for exactly this, in those words, and P9's three new endpoints landed in the catch-all behind them. The consequence seen in a browser: the generate screen offered "আগের ফলাফল — ১১২৫টি পিরিয়ড বসানো আছে" for a routine deleted from the database minutes earlier, with a button leading into a 404. Worse in principle is `/rms/setup`: a stale readiness checklist tells a school it is ready after someone emptied the room list, and the person believes it until the server refuses. | **P9-3** | MEDIUM | No | **RESOLVED 2026-09-06** — `/rms/setup`, `/rms/generate` and `/rms/assignments` joined the existing authoring-register carve-out in `sw-router.ts`; `surfaces.test.ts` pins all three, and pins that `/rms/routine` and `/academics/roster` still cache, so the fix could not become "make everything network-only". Verified in the browser: with the carve-out in place, a routine deleted behind the browser's back disappears on the next load. Recorded honestly — the original stale render was observed but not cleanly isolated to the service worker, and the carve-out stands on the P0 precedent rather than on that one reading. | P9-3 |
| **B-96** | **Four P9 CSS classes had no rule behind them** | `.ui-stack` (P9-1), `.ui-cell-line`, `.ui-cell-meta` and `.setup-period-row` (P9-2) were used as layout hooks in three views and defined nowhere in `app.css`. They rendered as bare block elements: on a 360px phone the bell-times editor became five full-width inputs touching each other, with the label, the two times, the kind and the remove button all stacked. Nothing was broken enough to fail a test — every assertion was about text — which is how it survived two phases. | **P9-3** | LOW | No | **RESOLVED 2026-09-06** — added to `app.css` beside `.ui-card-lead`, with a 360px-first two-column grid for the period row that widens to five columns at 640px. Verified in the browser at eight widths 360–1600: no horizontal overflow anywhere. | P9-3 |
| **B-97** | **`no_free_slot` was one code for four different problems** | The solver reports a single reason per unplaced demand, derived only from whether a room capability was involved. So "the section's week is already full", "the teacher is teaching elsewhere", "the teacher declared that hour unavailable" and "the other shift is using the only suitable room" all arrived as `no_free_slot` — four problems with four different fixes, presented as one. A coordinator could not tell which errand they were on, and the screen could not tell them, because the information was computed inside the placement loop and discarded on the way out. | **P9-4** | **HIGH** | No | **RESOLVED 2026-09-07** — `BlockerTally` records how many of the exhaustive pass's candidate hours each guard rejected, written only where a candidate has ALREADY been rejected so no placement decision changes (every solver suite passed unaltered). `src/explain.ts` turns the tally into one of eight categories with the count and the denominator in the sentence, and emits nothing where the tally is empty. 25 tests, pure. | P9-4 |
| **B-98** | **The F-503 explainer reported a hard-conflict count of zero as a constant** | `api/generation.ts:report` returned `hardViolations: 0` with a comment explaining that the three GiST exclusion constraints made a violation unstorable. That is the same claim P9-3 disproved: `rs_no_teacher_double_booking` and `rs_no_room_double_booking` carry `WHERE … routine_status = 'active'`, so a DRAFT — which is what a generated routine is — is protected on sections alone. P9-3 fixed `generate.ts`; this endpoint is the OTHER reader of the same routines and kept the constant. A coordinator opening the explainer on a draft with 40 stored room collisions was told there were none. | **P9-4** | **HIGH** | No | **RESOLVED 2026-09-07** — counted with the same one-pass window scan `generate.ts` uses, over the routine's own stored rows. | P9-4 |
| **B-99** | **A raw room-capability code reached the explainer screen from rows stored months earlier** | P9-3 found `"computer_lab" কক্ষে ৮০টি পিরিয়ড দরকার` on the generation screen and fixed the sentence `generate.ts` composes. The P9-4 audit found the same string reaching the F-503 explainer, read from `routines.soft_violations` where `solve.ts` had written it at solve time — so fixing the writer could never fix a row already in the database. `rooms.capabilities` is `text[]` with no vocabulary, so no lookup table can be complete either. | **P9-4** | MEDIUM | No | **RESOLVED 2026-09-07** — `src/presentation.ts` is one mapping AND one scrubber: known codes get their Bangla name, anything else becomes "বিশেষ কক্ষ" (less specific, and true), and stored sentences are rewritten on read. A school's own room codes ("R-1", "ভবন-২") are deliberately untouched — scrubbing those would be the opposite failure. 7 tests, including the `/g`-regex `lastIndex` trap that makes a shared pattern answer true, then false, then true. | P9-4 |
| **B-100** | **An 80-section school produced 1,516 findings** | Observed in the browser on the benchmark's large profile, deliberately over-subscribed: forty rows saying the same thing about the same subject, plus 1,358 soft trades one per teacher per rule. Close to a megabyte of JSON on a 2G connection and 1,516 interactive rows on a phone — a list nobody reads at all, which makes an explanation screen worse than none. | **P9-4** | **HIGH** | No | **RESOLVED 2026-09-07** — grouped server-side on two keys and only AFTER each finding's category is decided, so a group is genuinely one cause: unplaced demands on (category, subject), soft trades on their rule with every individual sentence kept one level in (F-505's "nothing is silently accepted" still holds). Two of a kind stay separate; grouping starts at three; two different walls under one subject stay apart. 1,516 → 12 rows, observed. A client-side cap of 25 per severity remains as a backstop. | P9-4 |
| **B-101** | **The explanation cost 38% of the run it was explaining** | The first `BlockerTally` asked each interval list twice — `overlaps` in the guard to decide, then `blockedBy` on the rejection path to find the owner — and allocated an array per rejected candidate hour. At 120 sections that is tens of thousands of extra scans and allocations: the college profile went from P9-3's 6.43s p95 to **8.89s**. An explanation must not be paid for by the thing it explains. | **P9-4** | MEDIUM | No | **RESOLVED 2026-09-07** — `IntervalBook.blockingOwner()` returns the answer `overlaps` would give AND the owner in one scan, allocating nothing; the member loop and the room-candidate loop stop at the first foreign holder. **6.78s p95**, about 5% over P9-3, measured with three repeats each from an empty routine. | P9-4 |
| **B-102** | **The findings card claimed "no problems found" from an empty list** | Caught by a P9-3 fixture that predates the `explanations` field, which is exactly the shape an older build or a truncated response would have: an empty array beside a summary saying twelve periods were missing, and the reassuring sentence was the only thing on screen that was wrong. An empty list is not the same as a clean run. | **P9-4** | MEDIUM | No | **RESOLVED 2026-09-07** — the claim is checked against `summary.unplacedPeriods` and `summary.hardConflicts` before it is made; where they disagree the screen says the explanation could not be loaded and repeats the number that IS known. | P9-4 |
| **B-103** | **The generate response was five sixths data nothing drew** | Measured after P9-4's grouping brought the finding COUNT down: the 120-section college profile still shipped **375 kB**. The blocker tallies were 175 kB of it and the solver's full soft-violation list — 1,172 sentences — most of the rest. The screen draws neither: the trades are in `explanations`, already grouped, and the complete list is served by the F-503 explainer from `routines.soft_violations` where the solver persisted it. On a 2G connection that is most of a minute for data nothing renders, and both are solver internals that §1 says not to expose. | **P9-4** | MEDIUM | No | **RESOLVED 2026-09-07** — both consumed server-side and dropped before the response is built; each shift now carries a `softViolations` count instead of the list. **375 kB → 70 kB** on the college, 73 → 19 kB on the small school. A test asserts `blockers`, `candidates`, `crossShiftNames` and `notEvaluated` never appear in the raw response body. | P9-4 |
| **B-104** | **The service worker's data cache was not partitioned by tenant** | `sw.ts`'s stale-while-revalidate did `cache.match(req)`, and a `Cache` matches on URL alone unless the stored response carries `Vary`. Ours do not. So the cached entry for `/api/v1/academics/hierarchy` — or `/ops/inbox`, `/ops/notices`, `/ops/calendar`, `/ops/branding`, `/rms/routine`, `/academics/roster` — was served to whoever asked next, regardless of which school's token was on the request. **This mattered because production addresses schools as `/app?tid=<uuid>` on ONE origin** (12-PRODUCTION-RUNBOOK §"What works today"; subdomains are documented as not ready and gated behind an attestation), so browser origin partitioning did not separate them. Observed, not theorised: during P9-4 browser acceptance a session for the benchmark school first painted against **মনিপুর স্কুল's academic year id**, cached moments earlier in the same browser — the readiness screen reported "এই শিক্ষাবর্ষে কোনো শাখা নেই" because RLS correctly returned nothing for the foreign year. RLS held; the CLIENT did not. Realistic devices: a Union Digital Centre, a shared office computer, a teacher working at two institutions. | **P9-4 (found), B-104 (fixed)** | **HIGH** | No | **RESOLVED 2026-09-07** — two mechanisms, both needed. **Tenant-aware cache keys** (`sw-router.ts:tenantCacheKey`, applied at the single point where `route()` flags every cached `/api/` decision) make a cross-tenant hit impossible to express, and hold during a switch before any purge has finished. **Purge on switch** (`local-data.ts:isTenantSwitch` + the existing B-8 `purgeLocalData`) removes the leaving school's data from the device; it runs at module top level in `app.ts`, synchronously and BEFORE `shikhon_tid` is overwritten, so the comparison still has its evidence and no fetch has been issued. Shell and media buckets deliberately unpartitioned — they are the product's own code, and keying them would re-download the app on the devices least able to afford it. `Vary: Authorization` rejected: correct, and it would disable the cache, since the token rotates every 15 minutes. Proof, real browser and real service worker: A's warm cache answers `null` for all three of B's identical reads; both schools coexist with different data; the switch clears the session and screen caches both ways while keeping the device id and theme; and **with the server stopped, A gets its own data offline while B's identical request fails rather than receiving A's**. 14 tests. The outbox needed no change — `ownedBy()` has scoped it by tenant since it was written. | B-104 |
| **B-105** | **The routine lock existed, was enforced, and no school could set it** | `routine_slots.is_pinned` has carried the comment "solver may not move it" since migration 006. `editor.ts:move` and `remove` have refused to touch a pinned slot for as long, `loadGrid` returned it and the grid rendered `data-pinned`. Nothing could ever WRITE it. The same shape P9-1 found in `section_subject_teachers` and P9-2 found in the bell schedule: a rule the product has and no institution can use. | **P9-5** | **HIGH** | No | **RESOLVED 2026-09-07** — `lock` / `unlock` actions on `POST /rms/editor`, no migration needed. Two UI defects fixed with it: `pick()` refused to SELECT a pinned slot, so its action bar never opened and the only control that could unlock it was unreachable (a lock was a one-way door); and the action row had "সরান" beside "পিন সরান", two controls a scanning eye reads as the same word — the unlock is now "পিন খুলুন". 18 API tests, 13 view tests. | P9-5 |
| **B-106** | **Two coordinators editing one routine silently overwrote each other** | `routine_slots.row_version` has been incremented by every editor mutation since A4 and checked by none of them. So the second writer won, the first was never told, and in a school office two people on one timetable is the ordinary case rather than the exception. | **P9-5** | **HIGH** | No | **RESOLVED 2026-09-07** — `requireVersion` refuses an edit made against a version the caller has not seen, on `assign`, `move`, `remove` and the new `lock`/`unlock`. Optional so callers that predate it keep working; the editor always sends one. The message does not name who changed it: `routine_slots` has no `updated_by`, and a sentence naming a person would be a claim the table cannot support. Proved live in a browser — two moves at one rendered version give `200` then `409 stale_slot`. | P9-5 |
| **B-107** | **`hasUnsavedChanges()` existed on two views and nothing ever called either** | The assignment matrix has had one since P9-1 and the routine editor gained one in this phase's first draft. Neither was reachable: `ShellRoute` had `mount` and `unmount` and no way to ask a view whether it was safe to leave. The same defect as B-105 in a different place — a control that exists, is correct, and nobody consults. | **P9-5** | MEDIUM | No | **RESOLVED 2026-09-07** — `ShellRoute.guardLeave(resume) => boolean`, optional, every other route unaffected. Blocking puts the address bar back, because the hash has already changed by the time the shell hears about it and otherwise it says the person is somewhere they are not. Both views wired. Its tests found a second thing: every `Shell` listens on the GLOBAL `hashchange`, so a shell left alive by an earlier test answered later tests' navigations — the existing shell tests never noticed because they only inspect the first render. 5 tests. | P9-5 |
| **B-108** | **A replacement routine generated while one is live was mostly empty, and could not be published at all** | Two defects. (1) `RmsSolver.loadExistingSlots` booked against every ACTIVE routine in the year on the assumption — written in its own comment — that the only other active routine is the other SHIFT. A school that publishes and then regenerates has one for the same shift, so the solver counted every teacher and room the outgoing timetable holds: **560 placed in v1, 193 in v2, 125 of 129 unplaced reading `no_free_slot`**. (2) `uq_routine_active` allows one active routine per (tenant, year, shift), and `supersedes_id` and the `superseded` status — both present since migration 006 — had **never been written** (0 rows of each in the live database), so publishing the replacement answered "আগে সেটি বদলান" with nothing that could. The database was never the blocker and says so in its own constraint comment. | P9-7 discovery | HIGH | No | **RESOLVED 2026-09-07.** The solver's busy-set now exempts the ACTIVE routine for the **same shift** — unique by `uq_routine_active`, derived from the schema rather than passed in, and other shifts still compete. Publishing demotes the predecessor and promotes the replacement in one transaction, writing `supersedes_id`. `POST /rms/generate` gained `baseline: 'inputs' | 'current'`; 'current' clones the live routine **including pinned slots** (`generated_by = 'copied'`, the third never-written control) and lets the same solver top up, and is refused if the bell schedule changed underneath it. **Measured: a replacement now places exactly what a first generation places at 20 / 40 / 80 / 120 sections; the clone path is 7.4×–11× faster.** Consumers read v1 throughout the draft's life and v2 the instant it lands, verified over real HTTP. 15 new API tests. | PHASE_LOG B-108 |
| **B-109** | **The 180 KB critical-path budget was full, and `demo.ts` was 10.7% of it** | `demo.ts` was **94.1 kB minified** — the single largest module in `app.js`, larger than the next four views combined — and every real school on a 2G connection downloaded a demonstration fixture they will never open. HEAD before P9-7 had 1,738 bytes of gzipped headroom; one screen costs about 2,000, so the gate went **401 bytes red** and the *next* screen would have failed too regardless of its size. P9-7's own view is 9.3 kB minified, the smallest of the routine screens, so there was no fat in it to cut. | P9-7 | HIGH | Was: CI `Critical-path size budget` | **RESOLVED 2026-09-07 (P9-7).** `demo.ts` is now its OWN bundle, `apps/pwa/public/demo.js`, following the precedent `platform.js` already set ("a school's device never downloads the console's code"). `DemoAuth` is constructed at exactly one seam inside an already-async `main()`, so the load is one `await` on a path only demo visitors take; `app.ts` holds a `import type` (erased) plus `await import('/demo.js')`, and the app build marks `/demo.js` **external** so esbuild leaves the import for the browser instead of inlining the module back. **app.js 184,721 → 159,352 bytes gzipped — 24,968 of headroom against the unchanged 180 KB limit.** Verified in the browser: a production `/app` load fetches `/app.js` and never `/demo.js`; `?demo=1` fetches it and all six demo roles work, on both demo tenants. `/demo.js` joins `UNHASHED_ENTRY_ASSETS` (stale-while-revalidate, not `cache-first` — it has no content hash and would otherwise match IMMUTABLE on its extension, the `/platform.js` pinning defect in a second place) and is deliberately **not** in `PRECACHE`, which would have put the bytes back by another route. The CI budget was not weakened. | PHASE_LOG P9-7 |
| **B-110** | **The migration probe could not detect a rolled-back 075** | `migration-status.mjs` checks a migration by the presence of a named object. 075 CHANGES an existing constraint — it widens `routine_edit_log_action_check` to admit `'resolve'` — and the constraint name is identical before and after, so running the rollback left the probe reporting "fully migrated". Found by running it. A probe that cannot fail is worse than no probe, because it is trusted. | **P9-6** | MEDIUM | No | **RESOLVED 2026-09-07** — a `constraint_def` kind that matches `pg_get_constraintdef` against a substring; 075 is registered as `routine_edit_log_action_check\|'resolve'`. Verified: the rollback now shows **MISSING**, 74/75, and re-applying restores it. | P9-6 |

## 4. POST-PILOT

| ID | Item | Why it matters | Phase | Priority | Blocker | Depends on | Status | Source |
|---|---|---|---|---|---|---|---|---|
| **B-17** | Object storage | Documents render and print correctly without it; what is missing is a stored PDF. Also blocks photo/voice homework submission (F-902). | R-5 / post-pilot | MEDIUM | No | Storage provider (**external**) | **DEFERRED** | PHASE_LOG R-5, R-8 |
| **B-18** | Board-registration index | Confirmed a sequential scan today. At 3–5 schools that is genuinely fine, and every index costs write throughput on the student import — the largest write in the product. The pilot produces the numbers that should decide it. | post-pilot | LOW | No | B-5 (real data volumes) | **DEFERRED** — deliberately, with a stated trigger | PHASE_LOG R-8 |
| **B-19** | The seven legacy student/guardian views | `my-attendance`, `results`, `assignments`, `fees`, `documents`, `learn`, `subjects` render themselves rather than using the P2 components. All are accessible, responsive and green across 48 browser configurations. Migrating a working screen is risk with no user-visible benefit. | P5/P8 | LOW | No | — | **OPEN** — listed rather than done, on purpose | PHASE_LOG P4 |
| **B-20** | Section chat | R-9's remaining item. Moderation and child-safety design is not started and is gated on pilot stability (D9). | R-9 | LOW | No | B-5 | **NOT STARTED** | Master Plan D9, R-9 |
| **B-38** | **Support mode / safe impersonation** | P7 §32 asked for a time-limited, marked, audited, read-only, tenant-scoped support session, and to defer it if the auth architecture could not carry it safely. It cannot, yet, and the reason is specific rather than general: **14 RLS policies key off `app.current_user_id()`** and 4 more off `app.my_section_ids()`. A platform admin has no `users` row inside the school, so those 18 policies would evaluate against an id that does not exist and return nothing — a support session would show a DIFFERENT screen from the one the person is calling about, which is worse than no support mode, because support would then debug a screen nobody is looking at. Making it show the same screen means adopting a real user's identity, which is the silent impersonation P7 forbids. A safe version needs either a marked support principal provisioned into the tenant, or an `app.support_actor()` concept threaded through those 18 policies — plus a decision about whether a support session may read decrypted PII at all. Schema and policy work, not console work. The read-only half is already solved and reusable: `SET LOCAL transaction_read_only = on` in `withTenant` cannot be forgotten by an endpoint. | P7 | MEDIUM | No | Owner decision on PII visibility | **DEFERRED — POST-PILOT.** P8 §19 asked for a classification: it is not a pilot blocker. A pilot is a handful of schools whose staff are reachable by phone, and the support question a pilot actually raises is answered by the platform console (institution state, services, portals, billing, audit) plus a call. Impersonation earns its risk at the scale where support cannot ring the head teacher. The blocker is unchanged and specific: 18 RLS policies key off `app.current_user_id()`/`app.my_section_ids()`, so a support session would render a DIFFERENT screen from the one being reported. | PHASE_LOG P7 §32, P8 §19 |

## 5. EXTERNAL DEPENDENCY

These cannot be closed by writing code. Listed separately so a reader never
mistakes "not done" for "not attempted".

| ID | Item | What is blocked | Owner action needed | Status |
|---|---|---|---|---|
| **B-1** | SMS aggregator contract | Real SMS delivery, SMS-based OTP login | Sign an aggregator | **BLOCKED** |
| **B-5** | A pilot institution | B-2, B-4, B-18, and the "under one hour onboarding" measurement | Commercial | **OPEN** |
| **B-17** | Object storage provider | Stored PDFs, photo/voice submissions | Choose a provider | **DEFERRED** |
| **B-21** | Data residency decision, PII master-key custody, MFS credentials | Reported as not-done on the readiness screen; none can be closed from the repository | Owner | **OPEN** (source: 07 §9j) |

## 6. OBSOLETE / SUPERSEDED

Kept, never deleted, so a reader who finds the old claim elsewhere can see how
it ended.

| ID | Item | Outcome | Date | Reference |
|---|---|---|---|---|
| **B-22** | "`GET /sync/pull` is unused — bug" | **RECLASSIFIED, not a bug.** Built, mounted, tested, working; no client calls it. That is an unused capability. Deleting it discards working tested code; wiring it up is a feature. It stays and stops being listed as a defect. | 2026-08-31 | PHASE_LOG R-8 cleanup audit |
| **B-23** | R-7.10 "Billing the schools is out of scope" | **SUPERSEDED by D16** from R-7 onward. Stands as written for R-7 itself. Implementation belongs to **P7**; P2–P6 must not build it. | 2026-09-01 | Master Plan D16 |
| **B-24** | "No audit viewer — backend complete, UI pending" | **RESOLVED** in R-3's completion pass. `audit-view.ts` exists and `audit` is in the navigation for principal and IT admin. The stale bullet survived in `07-IMPLEMENTATION-STATUS.md` §9e until D17's reconciliation found it contradicting §1 of the same file. | 2026-08-29, found 2026-09-01 | 07 §9e, corrected |
| **B-25** | "Guardian management is read-only" | **RESOLVED** in R-3's completion pass. `guardian-panel.ts` posts a new link and patches `canPayFees`. Same stale bullet as B-24. Unlinking is still missing and is tracked separately as **B-7**. | 2026-08-29, found 2026-09-01 | 07 §9e, corrected |
| **B-26** | Neon + Vercel as the deployed architecture | **SUPERSEDED in practice** by the VPS + Caddy + Docker-PostgreSQL deployment of 2026-08-31. The blueprint documents have **not** been rewritten, on purpose — see B-27. | 2026-08-31 | Master Plan §5b |

### Opened by the closure pass

| ID | Item | Why it matters | Phase | Priority | Blocker | Status | Source |
|---|---|---|---|---|---|---|---|
| **B-30** | The permission message on the remaining student screens | The closure pass fixed roster, marks and the guardian home. Auditing every student-accessible view in P5-0 found the same shape in **nine**, and **five different wordings** for one condition. Worse than the wording: a 403 left the refused data on screen, out of a cache. | P5-0 | LOW→HIGH once measured | No | **RESOLVED** 2026-09-01 · one `permissionMessage(subject?)` that `humanError` and `permissionState` both route through; `http-status.ts` carries the status through the throw; every screen drops its cache on a refusal and offers no retry. 23 tests. **Note:** on six of the nine a 403 is not currently reachable — those endpoints are RLS-scoped and answer with an empty payload, so the handling is defensive and is documented as such. | closure pass, P5-0 |
| **B-31** | `tsc -p .` does not cover `apps/pwa` | The root `tsconfig.json` **excludes** it. CI runs three configs; anyone running one locally typechecks the services and not the app. P4's, D17's and the closure pass's gate tables all recorded "TypeScript ×3" meaning three runs of one config. | P5-0 | MEDIUM | No | **RESOLVED** 2026-09-01 · `npm run typecheck` → `scripts/typecheck.mjs`, which **parses the CI workflow** for its config list so the two cannot drift, and fails when any new `.ts` appears outside every config. Verified in all three directions, including by catching its own author's new test file. | closure pass, P5-0 |

### Opened by P5-0

| ID | Item | Why it matters | Phase | Priority | Blocker | Status | Source |
|---|---|---|---|---|---|---|---|
| **B-35** | **DB test fixtures live at fixed uuids** | Every DB-backed suite builds its fixtures at hard-coded uuids and drops them in `after`. Two runs of the SAME suite against one database therefore delete each other's rows mid-test — reproduced on demand in P6, and the cause of P5's one non-reproducible `pass 12, fail 1`. **P6 fenced it**: a session-scoped `pg_advisory_lock` in all 26 suites turns the race into a wait. The deeper fix — per-run fixture ids — is not done, and is what would let these suites run in parallel rather than merely safely. | P6 | LOW | No | **FENCED, not eliminated** | PHASE_LOG P6 |
| **B-32** | **66 test files are typechecked by nothing** | **Re-measured 2026-09-08 (P11 audit): 53 `apps/pwa/test`, 8 `packages/ui-core/test`, 3 `services/sync-svc/test`, 2 `packages/offline/test` = 66, plus the 14 `/design` prototype files, for the 80 in the baseline.** It read *"46 test files — 34 `apps/pwa/test`, 7 `packages/ui-core/test`…"* and 73 errors when written; both grew as P6, P9 and P10 added suites, and P10's own measurement of the `apps/pwa` share alone found **113** errors, so the cost of closing this is higher than the row claimed, not lower. The 592 tests guarding the application are themselves unchecked, so a test can be quietly wrong about a type and still pass. Measured cost of closing it: `@types/jsdom` plus **73 pre-existing errors** in test code. (The other 14 unchecked files are the `/design` prototype, outside the product by D14.) Frozen by `scripts/typecheck-baseline.json`, so it cannot grow — the guard has since taken P5's, P6's and P9's new suites deliberately, 63 → 64 → 65 → 72 → 77 → 78 → **79** (P-ops added five service-side suites, which ARE checked; P9-1's `teaching-assignments-view.test.ts` and P9-7's `routine-publish-view.test.ts` are among those that moved this number, because `apps/pwa/test` is the directory no config covers). No tsconfig exclusion was weakened at any point. | P6 | MEDIUM | No | **OPEN** | P5-0 |
| **B-33** | `demo-gate.test.ts` skips `index.ts` | The derivation assumes `index.ts` is a dispatcher; `finance-svc` and `identity-svc` put real handlers in theirs, so a role gate added there would be invisible. **Verified by hand in P5-0: no gap today.** Automating it needs the `ROUTES` table parsed to map handler names to path segments. | P6 | LOW | No | **OPEN**, noted in the test | P5-0 |
| **B-44** | **Seven `ui-` class names are used in source and defined nowhere** | `ui-form` (structure-edit), `ui-status` (ui/badge), `ui-state-denied` (ui/feedback), `ui-filters-extra` (ui/filter), `ui-list-cell` and `ui-timeline-body` (ui/table), `ui-upload-input` (ui/upload). A class name that does not exist in `app.css` fails **silently** — the element renders unstyled and nothing anywhere says so. Found by probing all 142 `ui-` classes in `app.css` against every `className:` literal in `apps/pwa/src`. Two of my own cost a mis-rendered summary row this phase (`ui-filter-bar`, `ui-stack`), which is what prompted the probe. Some of these are probably deliberate markers with no styling; each needs a look before a guard can be written, and a baseline of seven unexamined entries is how a guard rots. | P-pilot-hardening | LOW | No | — | **OPEN** | P-pilot-hardening |
| **B-45** | **`teacher_leaves` and `teacher_availability` still have no writer** | M6 gave the substitute finder and the invigilator ranker a daily register (`teacher_attendance`, migration 063), which is the minimum useful loop. The two ORIGINAL tables both filters read are still written by nothing — so approved multi-day leave and recurring unavailability remain unexpressable, and a teacher on three weeks' maternity leave has to be marked absent every morning. Deliberately out of M6's scope: the brief said a minimum loop, not an HR module. The tables and both filters are correct and waiting. | P-pilot-hardening | MEDIUM | No | — | **OPEN** | PHASE_LOG P-pilot-hardening |

### Opened by P5

| ID | Item | Why it matters | Phase | Priority | Blocker | Status | Source |
|---|---|---|---|---|---|---|---|
| **B-34** | The Principal's other screens and the IT Admin screens | The audit found the LOOK was already canonical (P0 converged `.card` and `.ui-card`) and the gap was SHAPE: at 1440 every list screen was a phone layout stretched across a 1110px column. | **P5** | HIGH | No | **RESOLVED 2026-09-01.** Fourteen screens on the design system — academic (four depths, four tables, real crumbs), import (rebuilt: role-chosen kind, resolved year, error table), settings (two groups), system health (new vocabulary), fees · invoices · ledger · documents · publish · calendar · compose · inbox, plus users · students · audit · branding from the earlier pass. `results` and `inbox` keep their own markup as recorded exceptions with reasons. **Eleven defects fixed** — see PHASE_LOG; five were security or privacy, including a ledger screen that fabricated a school's accounts under a 403. 19,812 element checks across 7 widths × 2 themes × 2 tenants, 0 failures. | P5 |

### Opened by P9-7

| ID | Item | Why it matters | Phase | Priority | Blocker | Status | Source |
|---|---|---|---|---|---|---|---|
| **B-108** | **A replacement routine generated while one is live is mostly empty** | OBSERVED on a real 20-section school: v1 published with 560 slots, then a fresh generation placed **193**, with 125 of 129 unplaced demands reading `no_free_slot`. A published routine's slots are `routine_status = 'active'`, so `rs_no_teacher_double_booking` and `rs_no_room_double_booking` bind them at YEAR scope — the new draft cannot use any hour the live routine occupies. P9-7 did not cause this; it made it visible, because publishing became reachable. A school revising its timetable mid-year meets it immediately. The fix is a solver-input change: `generate` must treat the routine being replaced as not-a-constraint (the mechanism exists — `solve()` already takes `alsoBookedAgainst` for the cross-shift case, and this is its inverse). Deliberately out of P9-7's scope, which is publication, not generation. | P9-8 or later | **HIGH** | No | **OPEN** | PHASE_LOG P9-7 |
| **B-109** | **The 180 KB critical-path budget is full, and `demo.ts` is 10.7% of it** | `demo.ts` is **94.1 kB minified** — the single largest module in `app.js`, larger than the next four views combined — and every real school on a 2G connection downloads a demonstration fixture they will never open. HEAD before P9-7 had 1,738 bytes of gzipped headroom; one screen costs about 2,000, so the gate is now **401 bytes red** and the *next* screen would fail too regardless of its size. P9-7's own view is 9.3 kB minified, the smallest of the routine screens and smaller than `rooms-view.ts`, so there is no fat in it to cut; moving two composed sentences to the server (which was also a correctness fix — see PHASE_LOG) recovered 174 bytes and that is the end of what trimming can do. The fix: `DemoAuth` is constructed at exactly ONE seam (`const auth = demoMode ? new DemoAuth() : realAuth`), so building `demo.ts` as its own bundle and loading it only in demo mode would free roughly 19 kB gzipped — years of headroom. It is a build + boot-path change that interacts with the service worker's precache list, which is why it was not made inside P9-7. | P9-8 or a dedicated pass | **HIGH** | **Yes — CI `Critical-path size budget` fails** | **OPEN** | PHASE_LOG P9-7 |

### Opened by B-108

| ID | Item | Why it matters | Phase | Priority | Blocker | Status | Source |
|---|---|---|---|---|---|---|---|
| **B-110** | **No screen shows a school its routine history** | B-108 made `superseded` a state a school reaches every time it revises a timetable, and superseded versions are deliberately excluded from the publish screen — that is where a head publishes FROM, and a retired version would accumulate there every term. What replaced what is on the audit record (`rms.routine.publish` carries `supersededId` and `supersededVersion`) and in `routines.supersedes_id`, so nothing is lost; there is simply no screen that reads it. A head asking "what did the timetable look like last term, and who changed it" has no answer inside the product. | later | LOW | No | **OPEN** | PHASE_LOG B-108 |
| **B-111** | **The Bangla numeral face is unverified on Android** | B-108 §16 fixed Hind Siliguri's ambiguous digits (১ reads as ৮ at UI sizes) with a `unicode-range` face over U+09E6–09EF sourced from `local()`. It is **measured working on Windows** (Nirmala UI, digits 186.76 → 218.41 px while letters stay 101.67 px) and the Noto names in the list are the ones Android uses — but Android is the target device and this was verified on a desktop. A device where no named face matches falls through to Hind Siliguri, i.e. today's rendering, so the change cannot regress; what is unproven is whether it IMPROVES on Android. Needs one Android device, or a device-lab screenshot. | later | MEDIUM | No | **OPEN** | PHASE_LOG B-108 |

### Opened by P9-8

| ID | Item | Why it matters | Phase | Priority | Blocker | Status | Source |
|---|---|---|---|---|---|---|---|
| **B-112** | **No printable routine** | P9-8 delivered all eight role-specific routine views as SCREENS. What a Bangladeshi school actually pins to a noticeboard, hands a new student or sends home in a bag is a PRINTED class routine. | P9-9 | MEDIUM | No | **RESOLVED 2026-09-07 (P9-9).** A seventh document type — `routine_sheet` — in the existing `GET /api/v1/ops/document`, reusing R-5's letterhead, watermark, signature, escaping and page rules. No PDF library and no second pipeline (§13). It imports `readTimetable` from rms-svc rather than re-deriving authorisation, so a printed sheet cannot show an hour the screen would refuse, and `status = 'active'` makes §15 true without a check. **Orientation follows content**: portrait for section/teacher/room/student, landscape for institution/class/group/stream. **Pages split until no cell holds more than six lessons** — measured, because a 120-section college has only 4 classes and grouping by class gave a cell of THIRTY, a row taller than the sheet that no page-break rule can rescue; it now prints 120 clean single-section pages (worst cell 1, 100 ms), and a 20-section school prints 5, one per class. Verified by measuring the RENDERED document: page box 297mm x 210mm, `break-before: page`, and the `@media print` rules re-scoped and computed as `avoid` / `table-header-group` / `auto`. **A captured PDF was not possible** — `window.print()` opens a blocking dialog this environment cannot dismiss (see B-113). 26 new tests. | PHASE_LOG P9-9 |

### Opened by P9-9

| ID | Item | Why it matters | Phase | Priority | Blocker | Status | Source |
|---|---|---|---|---|---|---|---|
| **B-113** | **Print output has never been rasterised or put on paper** | P9-9 verified the printed routine by measuring the RENDERED document — page box 297mm × 210mm, `break-before: page`, and the `@media print` rules re-scoped to screen and computed as `avoid` / `table-header-group` / `auto`. That is genuinely more than reading CSS, and it is still not a print. `window.print()` opens a blocking OS dialog that wedged the renderer here, so no PDF was captured and no sheet came out of a printer. What remains unproven is the final rasterisation: where the page actually breaks on a 120-page booklet, whether the grey period column reads on a mono laser, and whether the Bangla renders at 10.5px on paper rather than on a screen. Needs one machine that can print to PDF headlessly, or one office printer. | pilot | MEDIUM | No | **RESOLVED 2026-09-07 (P9-9 redesign).** The tooling was already on the machine: Chrome (`--headless --print-to-pdf`, which honours `@page` and embeds fonts) and PyMuPDF 1.28.2 to rasterise and measure. Nothing added to the repo. All 20 sheets across the four benchmark profiles now print to real PDF, and **document pages equal printed sheets on every one**. That capture immediately found what computed style could not: EVERY class page was spilling onto a second and third sheet (5 doc pages -> 10 sheets; the college booklet 20 -> 59), because the letterhead, the stacked meta and R-5's 190px signature block spent 76mm of a 210mm page before an hour was drawn. The lesson is narrower than the bug: "I could not do it the way I first tried" is not "it cannot be done". |  PHASE_LOG P9-9 |
| **B-114** | **A booklet's orientation is chosen from the requested scope, not from the pages it produced** | `@page` is document-wide, so a booklet that may contain class pages takes landscape. A 120-section college's institution booklet therefore prints 120 per-section pages in landscape — usable, and more column room than portrait, but more paper than those sheets need. Fixing it means deciding the orientation after the split rather than before, which needs the CSS to be computed from the built pages instead of from the query string. | later | LOW | No | **RESOLVED 2026-09-07 (P9-9 redesign).** No longer reachable: a booklet splits into CLASS pages only — never into per-section pages — so every page in a landscape document is a dense page that wants landscape. The mismatch existed because a class too deep for a page used to fall back to one page per section; a class too wide now splits into further class pages of N sections, N computed from the school's own teaching-period count. |  PHASE_LOG P9-9 |
| **B-115** | **No routine sheet has come off a physical printer** | P9-9's redesign rasterises every sheet to a real PDF and proves one document page is one sheet of A4, with fonts embedded as real subsets. What a PDF still cannot settle is ink: whether the break band's `#e5e7eb` and the period column's `#f3f4f6` stay distinguishable on a school's mono laser, whether 11.5px Bangla conjuncts hold at 600dpi on cheap paper, and whether the 9mm page margin survives a printer's own unprintable edge. The CSS is asserted neutral — a test fails any hex whose channels differ by 40 — but tone on screen is not tone on paper. Needs one office printer and one look. | pilot | LOW | No | **NOT OBSERVED / EXTERNAL** (re-affirmed 2026-09-08, after the typography pass). The PDFs are real, rasterised and now measured in POINTS — 28 documents, one document page to one sheet of A4 on every one, no routine content below 10pt. What a PDF still cannot answer is INK: whether the near-neutral section tints separate on a mono laser, whether Bangla conjuncts at 11.25pt hold at 600dpi on cheap paper, and whether the 7mm page margin survives a printer's own unprintable edge. Needs one office printer and one look. Not claimed as passing. |   PHASE_LOG P9-9 REDESIGN |
| **B-116** | **The academic year prints Latin beside Bangla numerals** | A routine sheet reads `শিক্ষাবর্ষ: 2026 · সংস্করণ: ১` — two numeral systems on one line. Deliberate at both ends and still odd to look at: `academic_years.label` is the school's own free text and is left alone for the same reason P9-4 does not scrub room names, while a period label IS converted because it is the platform's own numbering wearing the school's words. The rule is defensible per field and the RESULT is inconsistent on the page. Either convert the year for display only, or decide the school owns its label and accept the mix — but decide it once, for every document, rather than per builder. | later | LOW | No | **RESOLVED 2026-09-08 (P9-9 closure).** Not a new rule — `num()` has done locale-aware digits since R-5 and `date()` already went through it. FOUR builders (report card, admit card, ID card, routine sheet) and two screens passed the year RAW past the helper sitting on the same line as the version it formatted correctly. Verified before changing, as the brief required: `academic_years.label` is free text (onboarding stores `input || year` from the browser clock, so the CI database holds both `২০২৬` and `2026`) but `toBanglaDigits` rewrites `[0-9]` and nothing else, so `2026-27` prints ২০২৬-২৭, a Bangla label is untouched and words survive. It survived because EVERY fixture already said `২০২৬` — a test that only feeds correct-looking input cannot catch a missing conversion. Six new tests sweep every builder with Latin, mixed and session-shaped years. `num(x, 'en')` deliberately still leaves what it is given, and that is asserted. |  PHASE_LOG P9-9 REDESIGN |

### Opened by P10

| ID | Item | Why it matters | Phase | Priority | Blocker | Status | Source |
|---|---|---|---|---|---|---|---|
| **B-117** | **No `apps/pwa` test file is type-checked** | The typecheck gate exists to make a type error fail something, and it covers `src/` and `dev/` only — every one of the 80 unchecked files in the baseline is a test. P10-7 added a view-test file and the gate correctly refused it, which is how this surfaced: the honest fix is a tsconfig that includes `test/**`, not a re-baseline. Measured rather than guessed — adding one produces **113 errors**, the bulk of them a missing `@types/jsdom` (a new devDependency) plus `Element` vs `HTMLElement` narrowing in existing tests. None of that is P10-sized and none of it is urgent: these files run in CI and a type error in them fails loudly at runtime rather than silently. It is recorded here rather than smuggled into a phase it does not belong to. | P10 | LOW | No | — | **SUPERSEDED by [B-32] — corrected 2026-09-08.** This is the same defect, already on this list since P5-0 and scoped wider: B-32 covers all unchecked test files across four workspaces, of which `apps/pwa/test` is the largest share. I opened this row during P10 without finding B-32 first, which is the exact duplication *"One row, one ID"* exists to stop. The row is kept because IDs are permanent; the work is tracked under **B-32**, whose figures this measurement has now corrected. | PHASE_LOG P10 |
| **B-118** | **The fleet query is linear in fleet size, on every console request** | P10-1 removed the console's quadratic term and the unpaginated 142 kB download; what is left is honest and still linear. `app.platform_fleet_ranked` grades EVERY tenant to decide which need attention, so the cost is fleet-wide no matter which page is asked for. Measured on the development database, DB time only, page size 25, and **the first measurement was taken on the wrong population**: that database held 262 tenants of which **241 were leaked `p7-gate` test fixtures with no students, users or payments** (see B-119). **Re-measured 2026-09-08 (P12 audit) and the recorded figures were ~4× too high.** They were taken with `EXPLAIN ANALYZE`, whose per-node instrumentation dominates a query with this many LATERAL and subquery nodes — the same trap P11 hit when its DB-side instrument reported 177 ms against a 104 ms round trip. Plain `\timing`, warm, synthetic fleets rolled back: **292 → 16 ms, 500 → 20 ms, 1000 → 38 ms, 2000 → 71 ms** — about **0.036 ms** per empty tenant, not 0.155. The linear SHAPE holds; the magnitudes do not. The superseded figures were 260 → 45 ms, 500 → 81 ms, 1000 → 155 ms, 2000 → 307 ms. But the **21 real** tenants in the same database cost **11.5 ms between them**, roughly **0.5 ms each**: a school with enrolments, users, payments and sessions is about **3×** an empty row, because the grade runs five correlated subqueries per tenant. So the linear extrapolation above is a **lower bound on empty rows, not a forecast for real schools**. Against real institutions the honest estimate is ~0.5 ms per school per query, and a console load makes three (page, summary, queue): **~150 ms at 100 schools, ~375 ms at 250, ~750 ms at 500, ~1.5 s at 1000**. `OFFSET 250` costs the same as `OFFSET 0` — paging is free, fleet size is not. Not a pilot concern at today's numbers. The fix when it is needed is a materialised grade refreshed on a schedule, deliberately NOT built now: a cache whose staleness semantics are undefined is worse than a slow query, and nothing today needs it. | P10 | LOW | No | — | **OPEN — trigger revised UP to roughly 1,000–1,500 REAL institutions** (2026-09-08). It was moved DOWN to 300–500 in the P11 audit on the strength of the inflated numbers above; with the instrument removed the same reasoning lands about 3× further out. Twice revised, both times because the measurement changed rather than the code. | PHASE_LOG P10, P12 audit |
| **B-119** | **`tenant-gate.test.ts` cannot delete the schools it creates, and has leaked 241 of them** | The suite creates two tenants per run through `app.create_tenant` and its `after()` hook runs `DELETE FROM tenants WHERE id IN ($1,$2)` on the **platform** connection. That DELETE matches zero rows and raises nothing: `tenant_self` is `USING (id = app.current_tenant())`, so `shikhon_platform` cannot even SELECT those rows, let alone delete them — the same policy this very file documents in its comment on `setStatus` (“`tenants` is not directly writable by the platform role”). The teardown has therefore been silently doing nothing since P7. **271 of the 292 tenants in the development database are its fixtures** (241 of 262 when this row was written on 2026-09-08; +30 in a day of test runs), at 2 per run, growing without bound. This is not cosmetic: it made P10's first scale numbers a measurement of empty rows (B-118), and a fixture leak in this area previously surfaced as an unrelated service's suite failing to start. **Not a one-line fix**, which is why it is here rather than in the phase: there is no `app.delete_tenant` — deliberately, the product never hard-deletes a school — and neither role the test holds can remove the rows, so the fix is either a definer teardown function scoped to test fixtures or an owner connection the suite does not currently have. Cleaning the existing 241 is a separate, deliberate act on a development database. | P7 | MEDIUM | No | — | **RESOLVED 2026-09-08 (pre-pilot pass).** The cause was exactly as diagnosed: the teardown ran `DELETE FROM tenants` on the PLATFORM pool, where `tenant_self` (`USING id = app.current_tenant()`) matched no rows because a bare platform query sets no current tenant. The same policy is what makes the fix work — each tenant is now dropped IN ITS OWN CONTEXT via `asBootstrap`, the shape every other suite already used. Proven rather than asserted: three consecutive runs of `tenant-gate.test.ts` left the count at 273 (it had been +2 per run), then the 273 accumulated fixtures were cleared and a FULL suite run went 21 → 21 tenants, zero residue. The development database is now 21 real tenants, down from 294. | PHASE_LOG P10, pre-pilot pass |
| **B-120** | **No session/device list, and no way to revoke one** | `user_sessions` is written on every login and never read back to a person. `identity-svc` exposes `otp/request`, `otp/verify`, `refresh`, `logout` and `activate` — `logout` ends THIS session, and there is no “where am I signed in” surface and no “sign out everywhere”. A head teacher whose phone is stolen cannot end that phone's access; the practical answer today is to wait for the refresh token to expire. **This is not new work invented here.** It is `FINAL-FULL-PROJECT-AUDIT-REPORT` §31's deliverable, proposed there as phase “P10a”, which was skipped when the roadmap renumbered — what shipped as P10 is §29's proposed P11. The P12 audit confirmed it absent in code. **The Master Plan does not authorize it as a phase, so it is recorded here and NOT built**; it maps onto the owner's stated secure-session requirement and is small (one list endpoint, one revoke, one screen). | §31 / P12 audit | MEDIUM | No | — | **RESOLVED 2026-09-08.** `GET /api/v1/auth/sessions`, `POST …/sessions/revoke`, `POST …/sessions/revoke-others` — three sub-paths on the existing identity dispatcher, plus a নিরাপত্তা screen. **No second authentication system:** nothing here mints, verifies or stores a credential; revocation is the same `revoked_at` UPDATE `logout` already performed, and the refusal is the check `refresh.ts` has always done. **Keyed by `device_id`, not by row id** — `refresh` rotates, so a session is a CHAIN whose live head moves, and revoking by row id races it: the id a screen listed is already superseded, the UPDATE matches nothing, and the phone keeps working while the screen says it stopped. Proven by a test that rotates first, and mutation-checked. **Self-service only** — every role manages exactly their own devices, which answers all of §5's prohibitions and adds no privilege; an administrator ending another person's session is a new power over an account, is not in the Master Plan, and was deliberately not invented. §7 respected: no fingerprinting, the stored IP is never returned, and the raw user agent is reduced to “Chrome · Windows”. 13 API tests + 2 SW tests; security probe 38 → 44. Browser-verified end to end: revoked device refresh 200 → 401, the remaining session still 200. | FINAL-FULL-PROJECT-AUDIT-REPORT §31; PHASE_LOG P12 audit, pre-pilot pass, B-120 |
| **B-121** | **A dead session reads as “something went wrong” with a retry that cannot work** | When the access token AND the refresh token are both dead, `authedFetch`'s refresh fails and the view falls to its generic error — “কিছু সমস্যা হয়েছে। আবার চেষ্টা করুন।” — whose retry button will never succeed. The honest answer is “your session has ended, please sign in again” and a route to the login screen. **Observed by the owner in a screenshot of the হাজিরা tab, and diagnosed rather than assumed**: the session in that browser was a test session whose access token had expired and whose refresh token had already been rotated, so both legs returned 401. Reproduced deliberately (dead access → 401, dead refresh → 401); with a live session the same screen loads its section list correctly, so this is a message-and-routing gap, not a broken screen. Low severity, but it is the state a returning user meets after a long absence — and after B-120, the state every REVOKED device meets. | Owner observation 2026-09-08 | LOW | No | — | **RESOLVED 2026-09-08.** Two halves. (1) `auth.ts` now distinguishes a refresh that is **refused** from one that merely **failed**: only 401/403 clear the session and fire `onSessionEnded(reason)`; a 5xx or a thrown `fetch` returns the stale token untouched. That negative half is the dangerous one — “clear the session whenever refresh fails” is the obvious repair and is a worse bug, because one bad minute would sign out every device that happened to refresh and cost each of them a new OTP. **Mutation-checked (twice):** deleting the status guard fails exactly the two transient-failure tests and nothing else, and collapsing the 403 mapping fails exactly the two 403 tests. **A third reason was removed because nothing could reach it** — `refresh.ts` matches on `revoked_at IS NULL`, so a device revoked via B-120 gets 401 `invalid_refresh_token` exactly as an expired one does; there is no `session_revoked` reply, and the obvious way to make `revoked` reachable (treat 403 as revocation) would have told somebody whose ROLE was removed that their phone had been signed out. `sessions.test.ts` now pins that 401 contract server-side, since the client's decision depends on it. (2) `app.ts` `showSessionEnded()` replaces the generic “কিছু সমস্যা হয়েছে। আবার চেষ্টা করুন।” — `role="alert"`, one focused action, and a different sentence per ending; a deactivated account gets its own heading and “লগইন স্ক্রিনে ফিরে যান” rather than a login that cannot work. **Nothing is lost:** the unsent IndexedDB outbox and the device id survive, verified in the browser (outbox 1 → 1, payload intact) — a teacher's morning register exists nowhere else, and a revoked session is not a reason to lose it. 8 unit tests; browser-verified across all five endings using the app's own boot, with the 401 case as the harness's positive control. | PHASE_LOG B-120, B-121 |

## 7. DOCUMENTATION DEBT

| ID | Item | Why it matters | Phase | Priority | Blocker | Status |
|---|---|---|---|---|---|---|
| **B-27** | Decide which architecture is the target, then reconcile `D1`, `06-DEPLOYMENT.md` and `12-PRODUCTION-RUNBOOK.md` | Production runs a VPS with a Docker PostgreSQL; those three documents describe Neon + Vercel. Both are currently true of different things and the drift is disclosed in Master Plan §5b. It must not be silently rewritten to match production — a reader must not be told the blueprint was always the VPS. The correction needs an owner decision about which is the *target*. | — | MEDIUM | No | **OPEN — needs an owner decision** |
| **B-28** | `shikhonbd.com` still appears in source comments, two operator-facing Bangla strings, the marketing footer's contact address and the default VAPID subject, while production serves `sikhon.systems` | No logic depends on it; the drift is in prose and defaults — except the VAPID `sub`, which is a real value in a real protocol sent to a push service. | P6/P8 | LOW | No | **RESOLVED 2026-09-02 (P8)** — 24 occurrences across 13 source files replaced, including the two Bangla strings that told an operator to configure DNS for a domain the company does not serve, and the VAPID subject. The test that pinned the old domain now asserts the property it was named for rather than the literal. Documentation keeps its historical references, per D17; the marketing footer is inside the frozen `index.html` and is untouched. |
| **B-29** | "Onboarded in under one hour" is **UNMEASURED** | The measurement machinery exists (`audit.platform_access` timestamps, `scripts/pilot-report.mjs`), and counts nothing that has not been designated a pilot. The only onboardings on record are seeded fixtures and the author's own walkthroughs. The target must not be claimed until B-5. | R-7/R-8 | — | No | **OPEN**, dependent on B-5 |
